---
phase: 08-reproducible-dependencies
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:

  - Dockerfile

autonomous: true
requirements:

  - LOCK-05

must_haves:
  truths:

    - "El Dockerfile usa uv sync --frozen --no-dev para instalar dependencias en el builder stage"
    - "El binario uv se copia desde ghcr.io/astral-sh/uv:0.11.17 (versión exacta pinada)"
    - "La imagen Docker se construye de forma determinística desde uv.lock"
    - "El runtime stage recibe el .venv construido en el builder stage"
    - "docker-compose.yml no referencia pip install directamente (delega al Dockerfile)"
  artifacts:
    - path: "Dockerfile"
      provides: "Build multi-stage con uv para instalación determinística"
      contains: "ghcr.io/astral-sh/uv:0.11.17"
  key_links:
    - from: "Dockerfile builder stage"
      to: "uv.lock"
      via: "uv sync --frozen --no-dev"
      pattern: "uv sync --frozen"
    - from: "builder stage"
      to: "runtime stage"
      via: "COPY --from=builder /app/.venv /app/.venv"
      pattern: "COPY --from=builder.*\\.venv"

---

<objective>
Reescribir el Dockerfile para usar uv en el builder stage, instalando dependencias desde uv.lock con uv sync --frozen --no-dev en lugar de pip install -r requirements.txt.

Purpose: Garantizar que la imagen Docker instala exactamente las mismas versiones que el lockfile especifica, sin que uv intente resolver o actualizar dependencias durante el build. La versión del binario uv está pinada (0.11.17) para reproducibilidad total.

Output: Dockerfile reescrito con patrón uv multi-stage; imagen construible con docker build .
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-reproducible-dependencies/08-RESEARCH.md
</context>

<interfaces>
Estructura actual del Dockerfile (extraída del archivo):

Dockerfile actual (35 líneas):

- FROM python:3.11-slim AS builder / WORKDIR /build
- COPY requirements.txt . / RUN pip install --no-cache-dir -r requirements.txt
- FROM python:3.11-slim AS runtime / WORKDIR /app
- COPY --from=builder /usr/local/lib/python3.11/site-packages ...
- COPY --from=builder /usr/local/bin ...
- COPY pyproject.toml requirements.txt ./ / COPY src ./src / COPY static ./static
- USER appuser / EXPOSE 5400 / HEALTHCHECK / CMD uvicorn

El nuevo Dockerfile:

- Reemplaza pip install por uv sync --frozen --no-dev
- Copia binario uv desde ghcr.io/astral-sh/uv:0.11.17
- Usa --mount=type=cache para caching de layers eficiente
- Copia .venv en lugar de site-packages
- Añade PATH="/app/.venv/bin:$PATH" en runtime

</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Tarea 1: Reescribir Dockerfile con patrón uv multi-stage y verificar docker-compose.yml</name>
  <files>Dockerfile</files>

  <read_first>

    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/Dockerfile — contenido actual que se reemplaza: 35 líneas, pip install en builder
    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/pyproject.toml — confirmar que no hay extras de producción adicionales
    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/docker-compose.yml — si existe, verificar que no referencia pip install directamente (debe delegar al Dockerfile)
    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/.planning/phases/08-reproducible-dependencies/08-RESEARCH.md — patrón 5 del Dockerfile con uv (líneas 196-247), variables de entorno UV_*, pitfalls 1-5
  </read_first>

  <action>
Reemplazar el contenido completo del Dockerfile con el siguiente patrón (extraído y verificado de la investigación, sección "Patrón 5: Dockerfile con uv"):

--- INICIO DEL DOCKERFILE ---
# syntax=docker/dockerfile:1

FROM python:3.11-slim AS builder
COPY --from=ghcr.io/astral-sh/uv:0.11.17 /uv /bin/uv

WORKDIR /app
ENV UV_PYTHON_DOWNLOADS=0 \
    UV_COMPILE_BYTECODE=1 \
    UV_LINK_MODE=copy

RUN --mount=type=cache,target=/root/.cache/uv \
    --mount=type=bind,source=uv.lock,target=uv.lock \
    --mount=type=bind,source=pyproject.toml,target=pyproject.toml \
    uv sync --frozen --no-dev --no-install-project

COPY pyproject.toml uv.lock ./
COPY src ./src
COPY static ./static

RUN --mount=type=cache,target=/root/.cache/uv \
    uv sync --frozen --no-dev

FROM python:3.11-slim AS runtime
WORKDIR /app

RUN groupadd --gid 1000 appuser \
    && useradd --uid 1000 --gid appuser --create-home appuser

COPY --from=builder /app/.venv /app/.venv
COPY --from=builder /app/src ./src
COPY --from=builder /app/static ./static

RUN mkdir -p /app/data /app/output \
    && chown -R appuser:appuser /app

ENV HOST=0.0.0.0 \
    PORT=5400 \
    PYTHONUNBUFFERED=1 \
    PATH="/app/.venv/bin:$PATH"

EXPOSE 5400
USER appuser

HEALTHCHECK --interval=30s --timeout=5s --start-period=10s --retries=3 \
  CMD python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:5400/api/languages', timeout=3)"

CMD ["python", "-m", "uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "5400"]
--- FIN DEL DOCKERFILE ---

Puntos clave a verificar en el Dockerfile escrito:

- UV_PYTHON_DOWNLOADS=0: evita que uv descargue Python propio (Pitfall 2 de la investigación)
- UV_LINK_MODE=copy: necesario con --mount=type=cache para evitar errores de hard-link cross-filesystem
- Primer RUN de uv sync usa --no-install-project: instala solo dependencias, no el proyecto (layer de cache separado)
- Segundo RUN de uv sync instala el proyecto completo (con el código fuente ya copiado)
- --frozen en ambos RUN: usa el lockfile sin intentar actualizarlo (Pitfall 1 de la investigación)
- Runtime stage: PATH incluye /app/.venv/bin para que los ejecutables del venv sean accesibles

NOTA sobre docker-compose.yml: Si existe docker-compose.yml en la raíz, inspeccionarlo para confirmar que no contiene instrucciones pip install directas (debe limitarse a definir servicios que usan la imagen construida por el Dockerfile). No se requiere modificar docker-compose.yml — solo confirmar que su contenido delega la instalación al Dockerfile (LOCK-05 se cumple a nivel de Dockerfile).

NOTA sobre .dockerignore: Verificar que .dockerignore NO incluye uv.lock ni *.lock (Pitfall 4 de la investigación). Inspeccionar .dockerignore antes de construir.

Hacer commit del Dockerfile:
  git add Dockerfile
  git commit -m "feat(phase-08): actualizar Dockerfile para usar uv sync --frozen"
  </action>

  <verify>
    <automated>grep -q "uv sync --frozen" /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/Dockerfile && grep -q "ghcr.io/astral-sh/uv:0.11.17" /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/Dockerfile && echo "OK: Dockerfile actualizado"</automated>
    <automated>if [ -f /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/docker-compose.yml ]; then grep -v "pip install" /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/docker-compose.yml > /dev/null && echo "OK: docker-compose.yml no contiene pip install directo"; else echo "OK: docker-compose.yml no existe (no requerido)"; fi</automated>
  </verify>

  <acceptance_criteria>

    - grep -q "ghcr.io/astral-sh/uv:0.11.17" Dockerfile devuelve exit 0 (versión uv pinada)
    - grep -q "uv sync --frozen --no-dev" Dockerfile devuelve exit 0 (instalación determinística)
    - grep -q "UV_PYTHON_DOWNLOADS=0" Dockerfile devuelve exit 0 (evita descarga de Python)
    - grep -q "UV_LINK_MODE=copy" Dockerfile devuelve exit 0 (compatibilidad con cache mounts)
    - grep -q "COPY --from=builder /app/.venv /app/.venv" Dockerfile devuelve exit 0 (copia venv al runtime)
    - grep -q 'PATH="/app/.venv/bin:' Dockerfile devuelve exit 0 (PATH apunta al venv)
    - grep -qv "pip install" Dockerfile devuelve exit 0 (pip eliminado del Dockerfile)
    - git ls-files Dockerfile | grep -q "Dockerfile" (commiteado)
    - Si docker-compose.yml existe: grep -v "pip install" docker-compose.yml no falla (LOCK-05: docker-compose.yml delega instalación al Dockerfile, no llama pip directamente)
    - docker build -t md-translate-test /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/. devuelve exit 0 (build exitosa — requiere que uv.lock exista, este plan depende de 08-01)
  </acceptance_criteria>

  <done>Dockerfile reescrito con patrón uv multi-stage. La imagen se construye usando uv sync --frozen --no-dev con uv 0.11.17 desde imagen oficial pinada. El runtime recibe el .venv del builder. docker-compose.yml confirmado como no-interferente (no llama pip directamente). docker build . exitoso desde la raíz del proyecto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                               | Description                                                        |
| -------------------------------------- | ------------------------------------------------------------------ |
| ghcr.io/astral-sh/uv:0.11.17 → /bin/uv | Imagen oficial de Astral; versión pinada evita cambios silenciosos |
| uv.lock → Docker builder               | --frozen garantiza que no se modifica el lockfile durante la build |

## STRIDE Threat Register

| Threat ID   | Category               | Component                            | Disposition   | Mitigation Plan                                                                             |
| ----------- | ---------------------- | ------------------------------------ | ------------- | ------------------------------------------------------------------------------------------- |
| T-08-03     | Tampering              | ghcr.io/astral-sh/uv con tag mutable | mitigate      | Pin a versión exacta 0.11.17 en COPY --from=; no usar :latest                               |
| T-08-04     | Tampering              | uv sync sin --frozen en Docker       | mitigate      | Ambos RUN de uv sync usan --frozen; sin este flag uv podría intentar actualizar el lockfile |
| T-08-05     | Elevation of Privilege | Contenedor ejecutando como root      | mitigate      | USER appuser (uid 1000) en runtime stage; mkdir/chown antes del cambio de usuario           |
| T-08-SC     | Tampering              | npm/pip/cargo installs               | accept        | No se instalan paquetes nuevos; uv instala desde lockfile verificado                        |
</threat_model>

<verification>
Verificaciones post-commit:

  grep -q "uv sync --frozen" Dockerfile
  grep -q "ghcr.io/astral-sh/uv:0.11.17" Dockerfile
  grep -qv "pip install" Dockerfile
  [ ! -f docker-compose.yml ] || grep -v "pip install" docker-compose.yml > /dev/null

Build de verificación (requiere uv.lock del Plan 01 — este plan depende de 08-01):
  docker build -t md-translate-test .
  docker run --rm md-translate-test python -c "import fastapi; import openai; print('OK')"
</verification>

<success_criteria>

- Dockerfile contiene el patrón uv multi-stage completo (uv 0.11.17, --frozen, --no-dev)
- pip install no aparece en el Dockerfile
- docker-compose.yml (si existe) no llama pip install directamente; delega instalación al Dockerfile
- docker build . completa exitosamente (requiere uv.lock presente, generado en 08-01)
- El contenedor arrancado ejecuta "import fastapi" sin error

</success_criteria>

<output>
Crear `.planning/phases/08-reproducible-dependencies/08-02-SUMMARY.md` cuando termine.
</output>
