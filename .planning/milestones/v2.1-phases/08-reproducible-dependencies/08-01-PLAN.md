---
phase: 08-reproducible-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - uv.lock
  - requirements.txt

autonomous: true
requirements:

  - LOCK-01
  - LOCK-02
  - LOCK-03

must_haves:
  truths:

    - "El archivo uv.lock existe en la raíz del proyecto"
    - "uv.lock está commiteado en git (git ls-files lo lista)"
    - "uv sync en un entorno limpio instala dependencias sin errores"
    - "requirements.txt es un artefacto derivado de uv.lock, generado con uv export"
  artifacts:
    - path: "uv.lock"
      provides: "Lockfile con versiones exactas de los 59 paquetes resueltos"
      min_lines: 100
    - path: "requirements.txt"
      provides: "Fallback pip-compatible derivado del lockfile"
      contains: "fastapi"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock (resuelve el grafo completo)"
      pattern: "uv\\.lock"
    - from: "uv.lock"
      to: "requirements.txt"
      via: "uv export --format requirements-txt --no-hashes"
      pattern: "fastapi"

---

<objective>
Generar el lockfile uv.lock desde pyproject.toml y regenerar requirements.txt como artefacto derivado del lockfile.

Purpose: Fijar las versiones exactas de las 59 dependencias directas e indirectas del proyecto para garantizar reproducibilidad total del entorno. requirements.txt pasa a ser un artefacto derivado (para usuarios sin uv), no la fuente de verdad.

Output: uv.lock commiteado en git; requirements.txt actualizado con versiones exactas derivadas del lockfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-reproducible-dependencies/08-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Tarea 1: Generar uv.lock desde pyproject.toml</name>
  <files>uv.lock</files>

  <read_first>

    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/pyproject.toml — fuente de verdad de dependencias; contiene las 9 dependencias directas y los extras [test] y [pdf]
    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/.gitignore — verificar que uv.lock NO está excluido (debe commitearse)
  </read_first>

  <action>
Desde la raíz del proyecto /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown, ejecutar:

  uv lock

Este comando lee pyproject.toml (setuptools como build-backend, requires-python=">=3.11") y resuelve el grafo completo de dependencias incluyendo los extras [test] y [pdf]. No requiere ninguna modificación de pyproject.toml ni de .gitignore (ya excluye .venv/ pero NO excluye uv.lock).

El resultado es uv.lock en la raíz del proyecto con versiones exactas de aproximadamente 59 paquetes. Este archivo DEBE commitearse en git.

Después de generarlo, añadirlo al índice git:
  git add uv.lock

No hacer commit todavía — la Tarea 2 completará los artefactos de este plan.

Nota: uv 0.11.4 (instalado localmente) es compatible sin breaking changes para uv lock/sync. El lockfile generado es válido.
  </action>

  <verify>
    <automated>test -f /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/uv.lock && echo "OK: uv.lock existe"</automated>
  </verify>

  <acceptance_criteria>

    - test -f uv.lock devuelve exit 0
    - wc -l uv.lock devuelve un número mayor que 100 (el lockfile de 59 paquetes tiene cientos de líneas)
    - grep -q "fastapi" uv.lock devuelve exit 0
    - grep -q "openai" uv.lock devuelve exit 0
    - git status muestra uv.lock como archivo nuevo sin trackear (o staged)
  </acceptance_criteria>

  <done>uv.lock existe en la raíz del proyecto con versiones exactas de todas las dependencias directas e indirectas, preparado para commit.</done>
</task>

<task type="auto" tdd="false">
  <name>Tarea 2: Regenerar requirements.txt como artefacto derivado del lockfile</name>
  <files>requirements.txt</files>

  <read_first>

    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/requirements.txt — estado actual: 8 líneas con pins mínimos (>=); será reemplazado por la salida de uv export
    - /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/uv.lock — debe existir (generado en Tarea 1)
  </read_first>

  <action>
Desde la raíz del proyecto, ejecutar:

  uv export --format requirements-txt --no-hashes > requirements.txt

Este comando lee uv.lock y genera requirements.txt con versiones exactas (==) de todas las dependencias. La flag --no-hashes omite los SHA-256 para que el archivo sea legible y usable directamente con pip. El resultado incluirá -e . (editable install del propio proyecto) al final — esto es correcto y esperado para entornos de desarrollo local.

El archivo resultante reemplaza los pins mínimos (>=) del requirements.txt actual con versiones exactas derivadas del lockfile. Esta es la nueva fuente de verdad pip-compatible.

Añadir al índice git:
  git add requirements.txt

Crear commit con ambos artefactos:
  git commit -m "feat(phase-08): añadir uv.lock y regenerar requirements.txt desde lockfile"

Nota importante: requirements.txt con -e . es solo para entornos locales sin uv. El Dockerfile NO usa este archivo — usa uv sync --frozen directamente (ver Plan 02).
  </action>

  <verify>
    <automated>grep -c "==" /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/requirements.txt</automated>
    <automated>cd /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown && uv sync --dry-run && echo "LOCK-02 OK"</automated>
  </verify>

  <acceptance_criteria>

    - test -f requirements.txt devuelve exit 0
    - grep -c "==" requirements.txt devuelve un número mayor que 5 (versiones exactas en lugar de pins >=)
    - grep -q "fastapi==" requirements.txt devuelve exit 0 (fastapi con versión exacta)
    - grep -q "openai==" requirements.txt devuelve exit 0
    - git ls-files uv.lock muestra "uv.lock" (commiteado)
    - git ls-files requirements.txt muestra "requirements.txt" (commiteado)
    - uv sync --dry-run devuelve exit 0 desde un entorno limpio (verifica que el lockfile es coherente)
  </acceptance_criteria>

  <done>requirements.txt contiene versiones exactas (==) derivadas de uv.lock, ambos archivos están commiteados en git. El entorno es reproducible con uv sync o pip install -r requirements.txt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                   | Description                                                                                      |
| -------------------------- | ------------------------------------------------------------------------------------------------ |
| PyPI → uv.lock             | uv resuelve paquetes desde PyPI; el lockfile captura los hashes SHA-256                          |
| uv.lock → requirements.txt | uv export deriva el fallback pip; cualquier edición manual del requirements.txt será sobrescrita |

## STRIDE Threat Register

| Threat ID   | Category   | Component                         | Disposition   | Mitigation Plan                                                                                  |
| ----------- | ---------- | --------------------------------- | ------------- | ------------------------------------------------------------------------------------------------ |
| T-08-01     | Tampering  | PyPI → uv.lock                    | mitigate      | uv.lock incluye hashes SHA-256 de cada wheel/sdist; verificación automática en uv sync           |
| T-08-02     | Tampering  | requirements.txt (edición manual) | mitigate      | Documentar en README que requirements.txt es artefacto derivado; regenerar siempre con uv export |
| T-08-SC     | Tampering  | npm/pip/cargo installs            | accept        | Esta fase no instala paquetes nuevos en el proyecto; uv es herramienta de sistema ya instalada   |
</threat_model>

<verification>
Verificaciones de fase completa (ejecutar después del commit):

  test -f uv.lock
  git ls-files uv.lock | grep -q "uv.lock"
  grep -c "==" requirements.txt
  uv sync --dry-run
  python -c "import fastapi; import openai; import deepl; print('OK')"
</verification>

<success_criteria>

- uv.lock existe en la raíz y está commiteado en git (git ls-files uv.lock = "uv.lock")
- requirements.txt contiene versiones exactas (==) de todas las dependencias
- uv sync desde un entorno limpio completa sin errores ni resolución adicional
- Los paquetes core (fastapi, openai, deepl, uvicorn) están en ambos archivos con versiones exactas

</success_criteria>

<output>
Crear `.planning/phases/08-reproducible-dependencies/08-01-SUMMARY.md` cuando termine.
</output>
