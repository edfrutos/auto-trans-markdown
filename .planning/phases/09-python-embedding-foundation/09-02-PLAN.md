---
phase: 09-python-embedding-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:

  - macos/MDTranslator.xcodeproj/project.pbxproj
  - macos/MDTranslator/Info.plist
  - macos/MDTranslator/Assets.xcassets/AppIcon.appiconset/Contents.json

autonomous: false
requirements:

  - BUNDLE-03
  - BUNDLE-05

must_haves:
  truths:

    - "El proyecto Xcode en macos/MDTranslator.xcodeproj compila sin errores con Xcode 26.5"
    - "El bundle ID es com.edefrutos.md-translator y el deployment target es macOS 14.0"
    - "El app name es MD Translator (CFBundleName en Info.plist)"
    - "El proyecto incluye una Copy Bundle Resources phase que copia python-bundle/ a python/ y src/+pyproject.toml+uv.lock a backend/ en Contents/Resources"
    - "La firma de código está configurada como ad-hoc (--sign -) para desarrollo local"
  artifacts:
    - path: "macos/MDTranslator.xcodeproj/project.pbxproj"
      provides: "Configuración completa del proyecto Xcode"
      contains: "com.edefrutos.md-translator"
    - path: "macos/MDTranslator/Info.plist"
      provides: "Metadatos del bundle macOS"
      contains: "CFBundleIdentifier"
    - path: "macos/MDTranslator/Assets.xcassets/AppIcon.appiconset/Contents.json"
      provides: "Placeholder del icono de la app"
      contains: "AppIcon"
  key_links:
    - from: "macos/MDTranslator.xcodeproj/project.pbxproj"
      to: "python-bundle/"
      via: "Copy Bundle Resources phase (destino: python/)"
      pattern: "python-bundle"
    - from: "macos/MDTranslator.xcodeproj/project.pbxproj"
      to: "src/"
      via: "Copy Bundle Resources phase (destino: backend/)"
      pattern: "backend"

---

<objective>
Crear el proyecto Xcode mínimo para la app macOS nativa MD Translator. El proyecto debe compilar sin errores con Swift 6.3.2 / Xcode 26.5, tener configurado el Bundle ID, deployment target, firma ad-hoc, y las fases de copia de recursos que empaquetan el bundle Python y el backend FastAPI en Contents/Resources.

Purpose: Sin el scaffold Xcode no hay forma de compilar ni testear el código Swift de Phase 9 (Plans 09-03). El proyecto es la infraestructura que permite integrar el bundle Python con la app Swift.
Output: macos/MDTranslator.xcodeproj/ completo y compilable; Info.plist con metadatos correctos; Assets.xcassets con AppIcon placeholder.
</objective>

<execution_context>
@/Users/edefrutos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/edefrutos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-python-embedding-foundation/09-CONTEXT.md
@.planning/phases/09-python-embedding-foundation/09-RESEARCH.md
@.planning/phases/09-python-embedding-foundation/09-PATTERNS.md

<interfaces>
<!-- Configuración esperada del proyecto Xcode — extraída de CONTEXT.md y RESEARCH.md -->

Decisiones bloqueadas que el proyecto DEBE reflejar (CONTEXT.md):

- D-08: formato .xcodeproj tradicional (no Package.swift puro), compatible con Xcode 26.5
- D-09: App name = "MD Translator" | Bundle ID = "com.edefrutos.md-translator" | Deployment target = macOS 14.0
- D-10: Intérprete Python accesible en Contents/Resources/python/ (Bundle.main.resourceURL + "python")
- D-11: Backend en Contents/Resources/backend/ (Swift usa currentDirectoryURL = Resources/backend/)
- D-12: site-packages dentro del intérprete: Resources/python/lib/python3.11/site-packages/

Xcode Copy Bundle Resources — destinos en Contents/Resources:

- python-bundle/ → Contents/Resources/python/
- src/ → Contents/Resources/backend/src/
- pyproject.toml → Contents/Resources/backend/pyproject.toml
- uv.lock → Contents/Resources/backend/uv.lock

Info.plist mínimo requerido (de RESEARCH.md Code Examples):

- CFBundleName = "MD Translator"
- CFBundleIdentifier = "com.edefrutos.md-translator"
- CFBundleVersion = "1"
- CFBundleShortVersionString = "3.0.0"
- LSMinimumSystemVersion = "14.0"
- NSPrincipalClass = "NSApplication"
- NSHighResolutionCapable = true

Firma ad-hoc: CODE_SIGN_IDENTITY = "-" | CODE_SIGN_STYLE = Manual (sin Apple Developer account)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Crear el proyecto Xcode en macos/MDTranslator.xcodeproj</name>
  <what-built>
    Esta tarea requiere acción manual en Xcode porque project.pbxproj es un formato binario-ASCII generado por Xcode que no debe escribirse a mano. Sin embargo, Claude configura los pasos exactos y verifica el resultado.
  </what-built>
  <read_first>

    - .planning/phases/09-python-embedding-foundation/09-CONTEXT.md — D-08, D-09 (formato, nombre, Bundle ID, deployment target)
    - .planning/phases/09-python-embedding-foundation/09-RESEARCH.md — "Recommended Project Structure" y "Info.plist mínimo"
    - .planning/phases/09-python-embedding-foundation/09-PATTERNS.md — sección "project.pbxproj"
  </read_first>
  <how-to-verify>
    Pasos exactos para crear el proyecto en Xcode 26.5:

    1. Abrir Xcode 26.5
    2. File → New → Project...
    3. Seleccionar: macOS → App
    4. Rellenar el formulario con los valores EXACTOS:
       - Product Name: MDTranslator
       - Team: None (sin developer account)
       - Organization Identifier: com.edefrutos
       - Bundle Identifier (resultante): com.edefrutos.md-translator → VERIFICAR que sea este valor exacto
       - Interface: SwiftUI
       - Language: Swift
       - NO marcar "Use Core Data" ni "Include Tests"
    5. Guardar en: [raíz del repo]/macos/ (crear directorio si no existe)
       El proyecto debe quedar en macos/MDTranslator.xcodeproj (NO en macos/MDTranslator/MDTranslator.xcodeproj)
    6. Una vez creado:
       a. Seleccionar el target MDTranslator → General → Deployment Info → Minimum macOS: 14.0
       b. Signing & Capabilities → Signing → Uncheck "Automatically manage signing"
       c. Code Signing Identity: Sign to Run Locally (o escribir "-" en Build Settings → CODE_SIGN_IDENTITY)
    7. Verificar en Project Navigator que los archivos básicos existen:
       - MDTranslatorApp.swift (generado por Xcode — será reemplazado en Plan 09-03)
       - ContentView.swift (generado por Xcode — será reemplazado en Plan 09-03)
       - Info.plist o configuración de Info.plist en build settings
       - Assets.xcassets con AppIcon
    8. Hacer un build inicial (Cmd+B) para verificar que compila sin errores

    Después de crear el proyecto, Claude configurará los Copy Bundle Resources phases y corregirá Info.plist si es necesario.
  </how-to-verify>
  <resume-signal>Escribe "proyecto creado" cuando el proyecto Xcode compile sin errores. Si hay problemas, describe el error.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configurar Info.plist, Copy Bundle Resources y firma ad-hoc</name>
  <files>macos/MDTranslator/Info.plist, macos/MDTranslator.xcodeproj/project.pbxproj</files>
  <read_first>

    - macos/MDTranslator/Info.plist — leer el fichero generado por Xcode para editarlo correctamente
    - macos/MDTranslator.xcodeproj/project.pbxproj — leer para entender la estructura actual antes de modificar
    - .planning/phases/09-python-embedding-foundation/09-RESEARCH.md — sección "Info.plist mínimo" y "Architectural Responsibility Map" (rutas D-10, D-11)
    - .planning/phases/09-python-embedding-foundation/09-PATTERNS.md — sección "Info.plist" con valores exactos
    - .planning/phases/09-python-embedding-foundation/09-CONTEXT.md — D-09 (Bundle ID, version), D-10, D-11, D-12 (rutas de recursos)
  </read_first>
  <action>
    Realizar las siguientes configuraciones concretas:

    1. INFO.PLIST — Verificar/establecer los valores exactos:
       - CFBundleName = "MD Translator" (con espacio — no "MDTranslator")
       - CFBundleIdentifier = "com.edefrutos.md-translator"
       - CFBundleVersion = "1"
       - CFBundleShortVersionString = "3.0.0"
       - LSMinimumSystemVersion = "14.0" (no "13.0" que es el default de Xcode 26.5)
       - NSPrincipalClass = "NSApplication"
       - NSHighResolutionCapable = true (boolean, no string)
       Si Xcode generó el Info.plist como entradas en Build Settings (INFOPLIST_KEY_*), editar esas entradas en project.pbxproj en lugar del fichero plist.

    2. COPY BUNDLE RESOURCES PHASE — Añadir al target MDTranslator en project.pbxproj:
       La phase "Copy Bundle Resources" existente (generada por Xcode) debe incluir las siguientes referencias de ficheros con sus destinos de copia:
       - python-bundle/ referenciado con destino en Resources/python/ (Destination = Resources, Subpath = "python")
       - Nota: python-bundle/ es un directorio en la raíz del repo — añadirlo como folder reference (azul en Xcode, no group amarillo)
       Para src/, pyproject.toml y uv.lock, añadir una nueva "Copy Files" build phase con:
       - Destination = Resources
       - Subpath = "backend"
       - Files: src/ (folder reference), pyproject.toml, uv.lock
       Estos ficheros están en la raíz del repo, no dentro de macos/ — el path en project.pbxproj debe ser relativo al proyecto (../../src, ../../pyproject.toml, ../../uv.lock).

    3. FIRMA AD-HOC — En Build Settings del target MDTranslator:
       - CODE_SIGN_IDENTITY = "-" (firma ad-hoc)
       - CODE_SIGN_STYLE = Manual
       - DEVELOPMENT_TEAM = "" (vacío — sin Apple Developer account)

    Si el proyecto usa GENERATE_INFOPLIST_FILE = YES (Xcode moderno), añadir las claves faltantes (CFBundleName, LSMinimumSystemVersion = 14.0, NSHighResolutionCapable) como INFOPLIST_KEY_* en Build Settings en lugar de editar un fichero plist separado.
  </action>
  <verify>
    <automated>grep -c 'com.edefrutos.md-translator' /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/macos/MDTranslator.xcodeproj/project.pbxproj && grep -c 'python-bundle' /Volumes/ESSAGER/__01.-Proyectos/auto-trans-markdown/macos/MDTranslator.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>

    - macos/MDTranslator.xcodeproj/project.pbxproj contiene la cadena "com.edefrutos.md-translator"
    - macos/MDTranslator.xcodeproj/project.pbxproj contiene la cadena "python-bundle" (referencia al directorio del bundle Python)
    - macos/MDTranslator.xcodeproj/project.pbxproj contiene la cadena "CODE_SIGN_IDENTITY" con valor "-"
    - Info.plist (o INFOPLIST_KEY_* en build settings) contiene CFBundleName = "MD Translator" (con espacio)
    - Info.plist (o LSMinimumSystemVersion en build settings) tiene deployment target 14.0
    - El proyecto compila sin errores con xcodebuild -project macos/MDTranslator.xcodeproj -scheme MDTranslator build CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO 2>&1 | tail -5 (última línea: "BUILD SUCCEEDED")
    - pytest tests/ -q -x pasa sin cambios (regresión Python)
  </acceptance_criteria>
  <done>Proyecto Xcode compilable con Bundle ID correcto, deployment target macOS 14.0, Copy Bundle Resources configurada para python-bundle/ y backend/, firma ad-hoc. BUNDLE-03 tiene la infraestructura Xcode que necesita para ser implementado en Plan 09-03.</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                       | Description                                                                   |
| ------------------------------ | ----------------------------------------------------------------------------- |
| filesystem → Xcode build       | Xcode lee project.pbxproj y copia recursos al bundle durante la compilación   |
| repo root → Contents/Resources | Los ficheros src/, pyproject.toml, uv.lock se copian como recursos del bundle |

## STRIDE Threat Register

| Threat ID   | Category               | Component                              | Disposition   | Mitigation Plan                                                                                                                                                                                                                    |
| ----------- | ---------------------- | -------------------------------------- | ------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-09-03     | Elevation of Privilege | uvicorn subprocess                     | mitigate      | El servidor uvicorn debe arrancar bound a 127.0.0.1 exclusivamente. Los argumentos CLI --host 127.0.0.1 se implementan en ServerManager.swift (Plan 09-03); este plan establece la infraestructura Xcode que los contendrá.        |
| T-09-04     | Information Disclosure | src/ copiado como recurso del bundle   | accept        | src/ no contiene secretos (no hay .env en el bundle, per D-03/D-11). Las API keys se inyectan en runtime via Process.environment en Phase 10. El bundle .app no es ejecutable por usuarios externos en Phase 9 (desarrollo local). |
| T-09-SC     | Tampering              | Xcode build (sin paquetes de terceros) | accept        | Phase 9 no añade Swift Package Manager dependencies. Sin riesgo de paquetes slopcheck.                                                                                                                                             |

</threat_model>

<verification>
Después de completar ambas tareas:

1. ls macos/MDTranslator.xcodeproj/project.pbxproj — fichero existe
2. grep 'com.edefrutos.md-translator' macos/MDTranslator.xcodeproj/project.pbxproj — al menos 1 coincidencia
3. grep 'python-bundle' macos/MDTranslator.xcodeproj/project.pbxproj — al menos 1 coincidencia
4. xcodebuild -project macos/MDTranslator.xcodeproj -scheme MDTranslator build CODE_SIGN_IDENTITY="-" CODE_SIGNING_REQUIRED=NO 2>&1 | grep -E 'BUILD SUCCEEDED|BUILD FAILED'
5. pytest tests/ -q -x — 148 tests pasan sin cambios

</verification>

<success_criteria>

- macos/MDTranslator.xcodeproj existe y compila sin errores con xcodebuild
- Bundle ID = com.edefrutos.md-translator, app name = "MD Translator", deployment target = macOS 14.0 (D-09)
- Copy Bundle Resources copia python-bundle/ → python/ y src/+pyproject.toml+uv.lock → backend/ en Contents/Resources (D-10, D-11, D-12)
- Firma ad-hoc configurada (sin Apple Developer account)
- Suite pytest existente verde (148 tests)

</success_criteria>

<output>
Crear `.planning/phases/09-python-embedding-foundation/09-02-SUMMARY.md` al completar
</output>
