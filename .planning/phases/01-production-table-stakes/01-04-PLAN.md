---
phase: 01-production-table-stakes
plan: 04
type: execute
wave: 4
depends_on:

  - "01-02"
  - "01-03"

files_modified:

  - static/index.html
  - static/css/app.css
  - static/js/app.js

autonomous: true
requirements:

  - GLOS-02
  - TM-03

must_haves:
  truths:

    - "Usuario expande panel Glosario y edita entradas desde la web"
    - "Guardar glosario persiste vía PUT /api/glossary"
    - "Usuario puede limpiar memoria de traducción con confirmación"
  artifacts:
    - path: static/index.html
      provides: "Glossary section + clear memory button"
      contains: "glossary-section"
    - path: static/js/app.js
      provides: "loadGlossary, saveGlossary, clearMemory"
      contains: "loadGlossary"
  key_links:
    - from: static/js/app.js
      to: /api/glossary
      via: "fetch GET/PUT"
      pattern: "/api/glossary"
    - from: static/js/app.js
      to: /api/memory
      via: "fetch DELETE"
      pattern: "/api/memory"

---

<objective>
Implementar panel glosario editable y botón limpiar memoria en UI web (GLOS-02, TM-03 UI, D-16, D-09).

Purpose: Gestión visual de terminología y mantenimiento TM sin CLI.
Output: Panel colapsable glosario, CRUD vía API, botón clear memory con confirm.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-production-table-stakes/01-UI-SPEC.md
@.planning/phases/01-production-table-stakes/01-CONTEXT.md
@.planning/phases/01-production-table-stakes/01-03-SUMMARY.md
@static/index.html
@static/css/app.css
@static/js/app.js
@.planning/phases/00-mvp-hardening/00-UI-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup y estilos panel glosario</name>
  <files>static/index.html, static/css/app.css</files>
  <read_first>.planning/phases/01-production-table-stakes/01-UI-SPEC.md, static/index.html</read_first>
  <action>
    Per 01-UI-SPEC.md:

    1. Insertar `<section id="glossary-section">` entre controles de idioma (línea ~87) y tabs.

    2. Toggle button `#glossary-toggle`, panel `#glossary-panel` collapsed by default.

    3. Tabla con columnas: Término, Traducción, No traducir, Acciones.

    4. Botones `#btn-add-glossary`, `#btn-save-glossary`.

    5. Añadir `#btn-clear-memory` en action bar junto a `#btn-download`.

    6. CSS: `.input-inline`, `.glossary-chevron`, dark mode variants matching `.input-select`.

    7. Mantener Plus Jakarta / teal tokens — no shadcn, no nuevas dependencias CDN.
  </action>
  <verify>
    <automated>grep -q 'glossary-section' static/index.html && grep -q 'btn-clear-memory' static/index.html && grep -q 'input-inline' static/css/app.css</automated>
  </verify>
  <done>HTML/CSS del panel y botón memory presentes y accesibles</done>

</task>

<task type="auto">
  <name>Task 2: Lógica JS glosario + clear memory</name>
  <files>static/js/app.js</files>
  <read_first>static/js/app.js, .planning/phases/01-production-table-stakes/01-UI-SPEC.md</read_first>
  <action>
    Per D-16, D-09, 01-UI-SPEC:

    1. Extender `state.glossary` { loaded, entries, dirty, expanded }.

    2. `loadGlossary()` — GET /api/glossary on init (after loadLanguages); flatten do_not_translate + pair for current source/target into table rows.

    3. `renderGlossaryTable()`, add/remove row handlers.

    4. `saveGlossary()` — assemble YAML structure per UI-SPEC mapping; PUT /api/glossary; showStatus feedback.

    5. `clearMemory()` — confirm dialog → DELETE /api/memory → showStatus.

    6. Wire toggle expand/collapse with aria-expanded.

    7. Optional low-cost fixes from 00-UI-REVIEW: disable translate until languages loaded; aria-busy on target-lang during fetch.

    8. No manual re-fetch before translate — pipeline reads glossary.yaml server-side on each request (saved file is source of truth).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const js=fs.readFileSync('static/js/app.js','utf8'); ['loadGlossary','saveGlossary','clearMemory','/api/glossary','/api/memory'].every(s=>js.includes(s)) || process.exit(1)"</automated>
  </verify>
  <done>JS wired; fetch calls present; UX feedback on save/clear</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary    | Description          |
| ----------- | -------------------- |
| browser→API | Glossary PUT from UI |

## STRIDE Threat Register

| Threat ID   | Category   | Component    | Disposition   | Mitigation Plan                                              |
| ----------- | ---------- | ------------ | ------------- | ------------------------------------------------------------ |
| T-01-04-01  | S          | saveGlossary | mitigate      | Server validates PUT body; client sends structured JSON only |
| T-01-04-02  | D          | clearMemory  | accept        | confirm() gate; local tool                                   |
</threat_model>

<verification>

- Manual: expand glossary, add entry, save, translate editor text containing term
- Manual: clear memory confirms and succeeds
- grep/static checks pass

</verification>

<success_criteria>

- GLOS-02: UI gestiona entradas glosario
- TM-03: Botón limpiar memoria funcional en web

</success_criteria>

<output>
After completion, create `.planning/phases/01-production-table-stakes/01-04-SUMMARY.md`
</output>
