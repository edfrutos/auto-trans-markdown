---
phase: 01-production-table-stakes
plan: 02
type: execute
wave: 2
depends_on:

  - "01-01"

files_modified:

  - src/memory.py
  - src/pipeline.py
  - src/main.py
  - .gitignore
  - tests/test_memory.py
  - tests/test_pipeline.py

autonomous: true
requirements:

  - TM-01
  - TM-02
  - TM-03

must_haves:
  truths:

    - "Segmentos ya traducidos se sirven desde SQLite sin llamar al proveedor"
    - "Nuevas traducciones se persisten automáticamente tras proveedor"
    - "Usuario puede vaciar TM vía DELETE /api/memory y md-translate memory clear"
  artifacts:
    - path: src/memory.py
      provides: "TranslationMemory SQLite WAL"
      exports: ["TranslationMemory", "make_key"]
    - path: data/translation_memory.db
      provides: "Persistencia TM (gitignored via data/)"
    - path: src/main.py
      provides: "DELETE /api/memory"
      contains: "/api/memory"
  key_links:
    - from: src/pipeline.py
      to: src/memory.py
      via: "lookup before translate_segments; store after"
      pattern: "TranslationMemory"
    - from: src/main.py
      to: src/memory.py
      via: "DELETE endpoint calls clear()"
      pattern: "memory.clear"

---

<objective>
Implementar memoria de traducción SQLite e integrarla en pipeline y API clear (TM-01, TM-02, TM-03 parcial API/CLI hook, D-06 a D-10).

Purpose: Reducir coste API y garantizar consistencia terminológica en segmentos repetidos.
Output: `src/memory.py`, pipeline con cache hits/misses, `DELETE /api/memory`, `data/` gitignored.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-production-table-stakes/01-CONTEXT.md
@.planning/phases/01-production-table-stakes/01-RESEARCH.md
@.planning/phases/01-production-table-stakes/01-01-SUMMARY.md
@src/pipeline.py

<interfaces>
From src/pipeline.py (plan 01):
```python
@dataclass
class TranslateOptions:
    target_lang: str
    source_lang: str | None = None
    dry_run: bool = False
    use_memory: bool = True
    memory_path: Path | None = None
    ...

def translate_markdown(content: str, options: TranslateOptions) -> TranslateResult: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TranslationMemory SQLite module</name>
  <files>src/memory.py, tests/test_memory.py, .gitignore</files>
  <read_first>.planning/phases/01-production-table-stakes/01-RESEARCH.md</read_first>
  <behavior>

    - Test: store + lookup same text/lang returns hit
    - Test: normalize collapses whitespace in hash key
    - Test: clear() removes all rows; count() returns 0
    - Test: different target_lang → miss
  </behavior>
  <action>
    Per D-06, D-07, D-10:

    1. Crear `src/memory.py` con schema de 01-RESEARCH.md, WAL mode, `make_key()`, clase `TranslationMemory(db_path: Path)`.

    2. Métodos: `lookup(items, source_lang, target_lang) -> (hits: dict[int,str], misses: list[tuple[int,str]])`, `store(batch)`, `clear() -> int`, `count() -> int`.

    3. Default path: `ROOT / "data" / "translation_memory.db"`; crear directorio `data/` on init.

    4. Añadir `data/` a `.gitignore`.

    5. Tests con tempfile/tmp_path fixture — no tocar DB real del dev.
  </action>
  <verify>
    <automated>pytest tests/test_memory.py -q</automated>
  </verify>
  <done>memory.py con lookup/store/clear testeado</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrar TM en pipeline + API clear</name>
  <files>src/pipeline.py, src/main.py, tests/test_pipeline.py, tests/test_api.py</files>
  <read_first>src/pipeline.py, src/memory.py</read_first>
  <behavior>

    - Test: segunda llamada translate_markdown mismo segmento → cache_hits=1, translate_segments no llamado para hit
    - Test: use_memory=False bypasses TM
    - Test: DELETE /api/memory → 200; subsequent translate is miss
  </behavior>
  <action>
    Per D-05, D-08, D-09:

    1. En pipeline: si use_memory, partition translatable via tm.lookup (pre-glossary text); translate_segments solo misses; merge hits+new; store post-translation text for misses; set cache_hits/cache_misses en TranslateResult.

    2. Respetar dry_run — no TM read/write.

    3. Añadir `DELETE /api/memory` en main.py → TranslationMemory.clear(); respuesta JSON `{"deleted": N}`.

    4. Crear `data/` en startup main.py si no existe (junto OUTPUT_DIR).

    5. Actualizar tests pipeline y API.
  </action>
  <verify>
    <automated>pytest tests/test_memory.py tests/test_pipeline.py tests/test_api.py -q</automated>
  </verify>
  <done>TM integrada en pipeline; endpoint clear funcional</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary       | Description    |
| -------------- | -------------- |
| API→filesystem | TM DB en data/ |

## STRIDE Threat Register

| Threat ID   | Category   | Component             | Disposition   | Mitigation Plan                   |
| ----------- | ---------- | --------------------- | ------------- | --------------------------------- |
| T-01-02-01  | I          | translation_memory.db | mitigate      | data/ gitignored; no static mount |
| T-01-02-02  | D          | DELETE /api/memory    | accept        | Local dev tool; auth en Phase 4   |
</threat_model>

<verification>

- pytest tests/test_memory.py tests/test_pipeline.py tests/test_api.py -q
- Segunda traducción idéntica no invoca mock provider en test

</verification>

<success_criteria>

- TM-01: SQLite persiste traducciones por hash+idiomas
- TM-02: Lookup antes de proveedor; store después
- TM-03: API clear operativo (UI en plan 04; CLI en plan 05)

</success_criteria>

<output>
After completion, create `.planning/phases/01-production-table-stakes/01-02-SUMMARY.md`
</output>
