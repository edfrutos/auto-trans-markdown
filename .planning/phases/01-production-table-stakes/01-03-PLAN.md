---
phase: 01-production-table-stakes
plan: 03
type: execute
wave: 3
depends_on:

  - "01-02"

files_modified:

  - src/glossary.py
  - src/pipeline.py
  - src/main.py
  - glossary.yaml
  - tests/test_glossary.py
  - tests/test_pipeline.py

autonomous: true
requirements:

  - GLOS-01
  - GLOS-03

must_haves:
  truths:

    - "glossary.yaml define términos DNT y traducciones fijas por par de idiomas"
    - "Glosario aplicado en translate_markdown usado por editor, archivo y lote"
    - "OpenAI recibe reglas en prompt; DeepL usa placeholders wrap/restore"
  artifacts:
    - path: src/glossary.py
      provides: "Load/save/apply glossary rules"
      exports: ["Glossary", "load_glossary", "save_glossary"]
    - path: glossary.yaml
      provides: "Archivo ejemplo version 1"
    - path: src/main.py
      provides: "GET/PUT /api/glossary"
      contains: "/api/glossary"
  key_links:
    - from: src/pipeline.py
      to: src/glossary.py
      via: "pre/post process on misses"
      pattern: "apply_glossary"
    - from: src/main.py
      to: glossary.yaml
      via: "GET/PUT read/write file"
      pattern: "glossary.yaml"

---

<objective>
Implementar glosario YAML e integrarlo en pipeline y API CRUD (GLOS-01, GLOS-03, D-11 a D-15).

Purpose: Terminología consistente en documentación técnica.
Output: `src/glossary.py`, `glossary.yaml` ejemplo, pipeline con pre/post glossary, endpoints GET/PUT.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-production-table-stakes/01-CONTEXT.md
@.planning/phases/01-production-table-stakes/01-RESEARCH.md
@.planning/phases/01-production-table-stakes/01-02-SUMMARY.md
@src/translator.py
@src/pipeline.py

<interfaces>
SYSTEM_PROMPT in src/translator.py — append glossary block dynamically from pipeline, do not edit constant permanently.

DeepL path uses translate_segments with pre-processed text; restore after batch returns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: glossary.py load/save/apply</name>
  <files>src/glossary.py, glossary.yaml, tests/test_glossary.py</files>
  <read_first>.planning/phases/01-production-table-stakes/01-RESEARCH.md, NOTEBOOK.md</read_first>
  <behavior>

    - Test: do_not_translate term preserved in apply_pre/apply_post
    - Test: pair rule en-es replaces "dashboard" → "panel"
    - Test: missing file returns empty Glossary without error
    - Test: save_glossary + load round-trip
    - Test: DeepL placeholder wrap/restore cycle intact
    - Test: build_openai_glossary_block contains DNT and pairs
  </behavior>
  <action>
    Per D-11, D-12, D-13, D-14:

    1. Crear `src/glossary.py` con dataclass `Glossary(do_not_translate: list[str], pairs: dict[str, dict[str,str]])`, `load_glossary(path)`, `save_glossary(path, glossary)`, `get_rules(glossary, source_lang, target_lang)`.

    2. Implementar `apply_pre(text, rules, provider) -> (text, state)` y `apply_post(text, state) -> text` con longest-match-first.

    3. DeepL: placeholders `⟦GLO{n}⟧`; OpenAI: `build_prompt_appendix(rules) -> str`.

    4. Crear `glossary.yaml` ejemplo con 2-3 entradas (NOTEBOOK samples).

    5. PyYAML safe_load/safe_dump only; max file size 256KB on load.

    6. Tests completos en tests/test_glossary.py.
  </action>
  <verify>
    <automated>pytest tests/test_glossary.py -q</automated>
  </verify>
  <done>glossary.py testeado; glossary.yaml ejemplo committed</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: Pipeline + API glossary integration</name>
  <files>src/pipeline.py, src/main.py, src/translator.py, tests/test_pipeline.py, tests/test_api.py</files>
  <read_first>src/pipeline.py, src/glossary.py, src/translator.py</read_first>
  <behavior>

    - Test: translate_markdown with glossary mock/fixture applies fixed term in output
    - Test: use_glossary=False skips glossary
    - Test: GET /api/glossary returns YAML structure as JSON
    - Test: PUT /api/glossary validates and persists
  </behavior>
  <action>
    Per D-05, D-15, GLOS-03:

    1. Pipeline: if use_glossary, load from options.glossary_path or ROOT/glossary.yaml; apply pre on misses before translate_segments; pass glossary appendix to OpenAI via new optional param `glossary_prompt: str | None` on translate_segments (minimal change — keyword-only, default None).

    2. Post-process provider results for misses; TM store uses post-glossary text (D-08).

    3. main.py: `GET /api/glossary` → JSON serializable Glossary; `PUT /api/glossary` body validated → save_glossary.

    4. Pydantic model `GlossaryPayload` for PUT validation.

    5. Integration test: POST /api/translate with fixture glossary ensures term replaced (mock provider).

    6. Do NOT embed glossary logic in parser.py.
  </action>
  <verify>
    <automated>pytest tests/test_glossary.py tests/test_pipeline.py tests/test_api.py -q</automated>
  </verify>
  <done>Glosario activo en pipeline y API; GLOS-01 y GLOS-03 satisfied</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                 | Description              |
| ------------------------ | ------------------------ |
| client→PUT /api/glossary | Untrusted YAML/JSON body |

## STRIDE Threat Register

| Threat ID   | Category   | Component         | Disposition   | Mitigation Plan                               |
| ----------- | ---------- | ----------------- | ------------- | --------------------------------------------- |
| T-01-03-01  | T          | PUT /api/glossary | mitigate      | safe_load only; 256KB limit; write fixed path |
| T-01-03-02  | I          | glossary.yaml     | accept        | Local project file; no secrets expected       |
</threat_model>

<verification>

- pytest tests/test_glossary.py tests/test_pipeline.py tests/test_api.py -q
- glossary.yaml committed as example

</verification>

<success_criteria>

- GLOS-01: glossary.yaml con DNT y pairs funcional
- GLOS-03: Mismos endpoints traducción aplican glosario vía pipeline

</success_criteria>

<output>
After completion, create `.planning/phases/01-production-table-stakes/01-03-SUMMARY.md`
</output>
