---
phase: 01-production-table-stakes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pipeline.py
  - src/main.py
  - tests/test_pipeline.py
autonomous: true
requirements:
  - PIPE-01
must_haves:
  truths:
    - "API, CLI y web pueden llamar translate_markdown() con el mismo contrato"
    - "Endpoints /api/translate, /file y /batch ya no duplican lógica segment→translate→reassemble"
    - "dry_run=True devuelve segmentos traducibles sin llamar al proveedor"
  artifacts:
    - path: src/pipeline.py
      provides: "Fachada translate_markdown + TranslateOptions + TranslateResult"
      exports: ["translate_markdown", "TranslateOptions", "TranslateResult"]
    - path: src/main.py
      provides: "Handlers delegando a pipeline"
      contains: "translate_markdown"
  key_links:
    - from: src/main.py
      to: src/pipeline.py
      via: "run_in_executor(translate_markdown, ...)"
      pattern: "translate_markdown"
    - from: src/pipeline.py
      to: src/parser.py
      via: "segment_markdown, collect_translatable, reassemble"
      pattern: "segment_markdown"
    - from: src/pipeline.py
      to: src/translator.py
      via: "translate_segments"
      pattern: "translate_segments"
---

<objective>
Crear `src/pipeline.py` como fachada única y refactorizar `src/main.py` para eliminar `_translate_file_content()` (PIPE-01, D-01 a D-05).

Purpose: Un solo punto de orquestación antes de añadir TM y glosario; evitar divergencia API/CLI.
Output: `TranslateOptions`, `TranslateResult`, `translate_markdown()`; main.py delega en todos los endpoints de traducción.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-production-table-stakes/01-CONTEXT.md
@.planning/phases/01-production-table-stakes/01-RESEARCH.md
@.planning/phases/00-mvp-hardening/00-04-SUMMARY.md
@src/main.py
@src/translator.py
@src/parser.py
@tests/conftest.py

<interfaces>
From src/translator.py:
```python
class ProgressCallback(Protocol):
    def __call__(self, done: int, total: int) -> None: ...

class IncompleteTranslationError(ValueError): ...

def translate_segments(
    items: list[tuple[int, str]],
    target_lang: str,
    source_lang: str | None = None,
    *,
    on_progress: Callable[[int, int], None] | None = None,
    client=None,
) -> dict[int, str]: ...

def is_valid_target_lang(code: str) -> bool: ...
def is_valid_source_lang(code: str) -> bool: ...
```

From src/parser.py:
```python
def segment_markdown(content: str) -> list[Segment]: ...
def collect_translatable(segments: list[Segment]) -> list[tuple[int, str]]: ...
def reassemble(segments: list[Segment], translations: dict[int, str]) -> str: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TranslateOptions, TranslateResult y translate_markdown base</name>
  <files>src/pipeline.py, tests/test_pipeline.py</files>
  <read_first>src/main.py, src/translator.py, src/parser.py, .planning/phases/01-production-table-stakes/01-CONTEXT.md</read_first>
  <behavior>
    - Test: translate_markdown con mock translate_segments devuelve content reassembled y counts correctos
    - Test: dry_run=True no invoca translate_segments; devuelve dry_run_segments igual a collect_translatable
    - Test: target_lang inválido lanza ValueError
    - Test: IncompleteTranslationError propaga sin capturar
  </behavior>
  <action>
    Per D-01, D-02, D-03, D-05 (sin TM/glossary aún — stubs para hooks):

    1. Crear `src/pipeline.py` con `@dataclass TranslateOptions` (target_lang, source_lang=None, dry_run=False, use_memory=True, use_glossary=True, glossary_path=None, memory_path=None, on_progress=None) y `@dataclass TranslateResult` (content, segments_total, segments_translated, cache_hits=0, cache_misses=0, dry_run_segments=None).

    2. Implementar `translate_markdown(content, options) -> TranslateResult`:
       - Validar idiomas con is_valid_target_lang / is_valid_source_lang (source None o "auto" → source=None para translator)
       - segment_markdown → collect_translatable
       - Si dry_run: return TranslateResult(content="", segments_total=len(segments), segments_translated=len(translatable), dry_run_segments=translatable)
       - Else: translate_segments(translatable, ...) → reassemble → return result con cache_hits=0, cache_misses=len(translatable) (placeholder hasta plan 02)

    3. No añadir TM ni glossary en este plan — dejar comentarios `# TM hook (plan 02)` / `# Glossary hook (plan 03)`.

    4. Crear tests/test_pipeline.py con mocks de translate_segments.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline.py -q</automated>
  </verify>
  <done>translate_markdown funciona end-to-end sin TM/glossary; tests pasan</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar main.py para usar pipeline</name>
  <files>src/main.py, tests/test_api.py, tests/test_integration.py</files>
  <read_first>src/main.py, tests/test_api.py, tests/conftest.py</read_first>
  <action>
    Per D-04:

    1. Eliminar `_translate_file_content()`.

    2. Añadir helper `_run_translate(content, target_lang, source_lang, dry_run=False) -> TranslateResult` que construye TranslateOptions y llama `translate_markdown` vía run_in_executor.

    3. Actualizar `translate_text`, `translate_file`, `translate_batch` para usar `_run_translate`; mapear TranslateResult a TranslateResponse (content, segments_total, segments_translated).

    4. Mantener manejo existente de IncompleteTranslationError, RuntimeError, HTTPException.

    5. Actualizar tests que mockeaban `_translate_file_content` o translate_segments directamente — mockear pipeline.translate_markdown o mantener mock translate_segments si tests pasan sin cambio.

    6. Verificar suite Phase 0 intacta: pytest tests/ -q
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <done>Ningún endpoint usa _translate_file_content; todos los tests existentes pasan</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Markdown content en POST body/upload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | translate_markdown | accept | Sin auth en v1 local; sin cambio de superficie |
| T-01-02 | D | main.py executor | mitigate | Propagar IncompleteTranslationError; no reassemble parcial |
</threat_model>

<verification>
- `pytest tests/ -q` — suite completa verde
- grep confirma ausencia de `_translate_file_content` en src/
</verification>

<success_criteria>
- PIPE-01: translate_markdown() existe y es usada por los 3 endpoints de traducción
- Comportamiento idéntico al MVP Phase 0 (mismos status codes, mismos campos respuesta)
</success_criteria>

<output>
After completion, create `.planning/phases/01-production-table-stakes/01-01-SUMMARY.md`
</output>
