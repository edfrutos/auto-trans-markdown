---
phase: 01-production-table-stakes
plan: 05
type: execute
wave: 4
depends_on:

  - "01-03"

files_modified:

  - src/cli.py
  - pyproject.toml
  - requirements.txt
  - tests/test_cli.py
  - README.md

autonomous: true
requirements:

  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05

user_setup: []
must_haves:
  truths:

    - "md-translate file traduce un .md a archivo de salida"
    - "md-translate dir traduce recursivamente preservando estructura"
    - "md-translate batch genera ZIP o directorio de salida"
    - "--dry-run lista segmentos sin llamar al proveedor"
    - "md-translate apunta a Typer; serve arranca servidor web"
  artifacts:
    - path: src/cli.py
      provides: "Typer app con file|dir|batch|serve|memory"
      exports: ["app"]
    - path: pyproject.toml
      provides: "md-translate = src.cli:app"
      contains: "src.cli:app"
  key_links:
    - from: src/cli.py
      to: src/pipeline.py
      via: "translate_markdown for all translate commands"
      pattern: "translate_markdown"
    - from: src/cli.py
      to: src/main.py
      via: "serve subcommand calls run()"
      pattern: "from src.main import run"

---

<objective>
Implementar CLI Typer `md-translate` con subcomandos file/dir/batch/serve/memory y corregir entry point (CLI-01 a CLI-05, D-17 a D-25).

Purpose: Automatización CI/local sin servidor web.
Output: `src/cli.py`, pyproject/requirements updated, tests CliRunner.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-production-table-stakes/01-CONTEXT.md
@.planning/phases/01-production-table-stakes/01-RESEARCH.md
@.planning/phases/01-production-table-stakes/01-03-SUMMARY.md
@src/pipeline.py
@src/main.py
@pyproject.toml
@README.md

<interfaces>
```python
# src/pipeline.py
def translate_markdown(content: str, options: TranslateOptions) -> TranslateResult: ...

# Entry target
# pyproject.toml: md-translate = "src.cli:app"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Typer app — file, dry-run, memory clear, serve</name>
  <files>src/cli.py, pyproject.toml, requirements.txt, tests/test_cli.py</files>
  <read_first>.planning/phases/01-production-table-stakes/01-RESEARCH.md, src/pipeline.py</read_first>
  <behavior>

    - Test: md-translate file --dry-run exits 0; stdout mentions segment count; translate_segments not called (mock pipeline)
    - Test: md-translate file con mock pipeline writes output file
    - Test: md-translate memory clear exits 0 (mock TranslationMemory.clear)
    - Test: md-translate serve invocable (mock run())
    - Test: invalid target_lang exits 2
  </behavior>
  <action>
    Per D-17, D-18, D-19, D-20, D-23, D-24, D-25, CLI-01, CLI-04, CLI-05:

    1. Crear `src/cli.py` con Typer `app`; `load_dotenv()` at import.

    2. `file` command: INPUT path, `-t/--target`, `-o/--output`, `-s/--source` default auto, `--dry-run`, `--no-memory`, `--no-glossary`, `--glossary-path`.

    3. `--dry-run`: call translate_markdown(dry_run=True); print JSON lines `{index, text}` per segment to stdout.

    4. `memory clear` subcommand: TranslationMemory.clear(); print deleted count.

    5. `serve` subcommand: import run from src.main.

    6. Exit codes: 0 OK, 1 translation/IncompleteTranslationError/RuntimeError, 2 typer/validation errors.

    7. Update pyproject.toml scripts + dependencies (typer, pyyaml); mirror requirements.txt.

    8. tests/test_cli.py with typer.testing.CliRunner.
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q</automated>
  </verify>
  <done>file, dry-run, memory clear, serve testeados; entry point corregido</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: dir y batch subcomandos + README</name>
  <files>src/cli.py, tests/test_cli.py, README.md</files>
  <read_first>NOTEBOOK.md §3, src/cli.py</read_first>
  <behavior>

    - Test: dir --recursive traduce 2 nested .md files preserving relative paths (mock pipeline)
    - Test: batch --zip creates zip with translated files
    - Test: batch --output-dir writes flat/nested outputs
    - Test: non-.md files skipped in dir mode
  </behavior>
  <action>
    Per D-21, D-22, CLI-02, CLI-03:

    1. `dir` command: PATH, `-o/--output-dir`, `-t`, `-s`, `--recursive`, shared flags.

    2. Preserve relative path structure under output-dir when recursive.

    3. `batch` command: accept multiple PATH args (globs expanded by shell); `--zip PATH` xor `--output-dir PATH` required.

    4. Reuse shared helper `_build_options(...) -> TranslateOptions` and `_translate_path(path) -> TranslateResult`.

    5. Skip non-markdown extensions; collect errors — exit 1 if any file fails, 0 if all OK.

    6. Update README.md CLI section with examples from 01-RESEARCH.md.

    7. Full suite: pytest tests/ -q
  </action>
  <verify>
    <automated>pytest tests/test_cli.py tests/ -q</automated>
  </verify>
  <done>dir y batch operativos; README documentado; suite completa verde</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary       | Description                |
| -------------- | -------------------------- |
| CLI→filesystem | Reads user-specified paths |

## STRIDE Threat Register

| Threat ID   | Category   | Component   | Disposition   | Mitigation Plan                                   |
| ----------- | ---------- | ----------- | ------------- | ------------------------------------------------- |
| T-01-05-01  | E          | dir/batch   | mitigate      | Resolve paths; only .md/.markdown/.mdx extensions |
| T-01-05-02  | I          | file read   | accept        | Local CLI tool; user controls paths               |
</threat_model>

<verification>

- pytest tests/ -q — all tests pass
- `pip install -e .` then `md-translate --help` shows subcommands

</verification>

<success_criteria>

- CLI-01 through CLI-05 satisfied
- md-translate entry point is src.cli:app; serve starts web server

</success_criteria>

<output>
After completion, create `.planning/phases/01-production-table-stakes/01-05-SUMMARY.md`
</output>
