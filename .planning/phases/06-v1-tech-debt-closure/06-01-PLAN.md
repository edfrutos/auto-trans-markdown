---
phase: 06-v1-tech-debt-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - src/cli.py
  - tests/test_cli.py

autonomous: true
requirements:

  - DEBT-01

must_haves:
  truths:

    - "batch --zip propaga --tone igual que batch sin zip"
  artifacts:
    - path: src/cli.py
      provides: "tone en rama zip de batch_cmd"
  key_links:
    - from: src/cli.py batch_cmd
      to: _build_options
      via: "tone argument en ZIP loop"

---

<objective>
Paridad CLI: `batch --zip` aplica `--tone` (DEBT-01).

Purpose: Cerrar gap de una línea detectado en audit v1.0.
Output: Fix + test de regresión.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-v1-tech-debt-closure/06-CONTEXT.md
@.planning/phases/06-v1-tech-debt-closure/06-RESEARCH.md
@src/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pasar tone en rama ZIP</name>
  <files>src/cli.py</files>
  <action>

    1. En `batch_cmd`, rama `if zip_path:` (~296), añadir `tone` como último arg a `_build_options` — idéntico a rama `else` (~328).
    2. Verificar que `batch_cmd` ya recibe `--tone` del Typer option (fase 5).
  </action>
  <verify>
    <automated>grep -A2 'if zip_path:' src/cli.py | grep -q tone || grep 'zip_path' -A20 src/cli.py | grep tone</automated>
  </verify>
  <done>ZIP branch llama _build_options(..., tone)</done>

</task>

<task type="auto">
  <name>Task 2: Test batch --zip --tone formal</name>
  <files>tests/test_cli.py</files>
  <action>

    1. Fixture o test `test_batch_zip_passes_tone`: monkeypatch `translate_markdown` captura `options.tone`.
    2. Invocar `batch doc.md -t es --zip out.zip --tone formal`.
    3. Assert `options.tone == "formal"` al menos una invocación.
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q -k batch_zip_tone</automated>
  </verify>
  <done>Test verde confirma propagación tone</done>

</task>

</tasks>
