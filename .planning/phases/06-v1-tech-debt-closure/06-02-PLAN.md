---
phase: 06-v1-tech-debt-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:

  - src/main.py
  - src/deployment.py
  - static/index.html
  - static/css/app.css
  - static/js/app.js
  - tests/test_api.py
  - tests/test_deployment.py

autonomous: true
requirements:

  - DEBT-02

must_haves:
  truths:

    - "fetch API incluye Authorization Bearer cuando hay token guardado"
    - "EventSource SSE incluye access_token query cuando API_TOKEN activo"
    - "Endpoints SSE/download/cancel exigen auth si API_TOKEN configurado"
  artifacts:
    - path: static/js/app.js
      provides: "getApiToken, apiFetch, authEventSourceUrl"
    - path: src/main.py
      provides: "_require_api_token con query access_token"
  key_links:
    - from: static/js/app.js
      to: /api/translate/*
      via: "apiFetch wrapper"
    - from: static/js/app.js
      to: /api/translate/batch/jobs/{id}/events
      via: "EventSource URL + access_token"

---

<objective>
UI Bearer token + SSE auth cuando servidor usa `API_TOKEN` (DEBT-02).

Purpose: Completar wiring de fase 4 SEC-02 para uso real en despliegue.
Output: Backend query-token auth, UI settings, tests 401/200.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-v1-tech-debt-closure/06-CONTEXT.md
@.planning/phases/06-v1-tech-debt-closure/06-RESEARCH.md
@src/deployment.py
@static/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend auth query param + proteger rutas GET job</name>
  <files>src/main.py, src/deployment.py, tests/test_deployment.py, tests/test_api.py</files>
  <action>

    1. Refactor `_require_api_token` to accept `authorization: Header` AND `access_token: Query(None)`.
    2. Verify: if `get_api_token()` is None → pass; else require Bearer header OR matching query token.
    3. Add `Depends(_require_api_token)` to: `batch_job_events`, `download_batch_translation_job`, `cancel_batch_translation_job`.
    4. Tests: events 401 without token when API_TOKEN set; 200 with `?access_token=`; Bearer on POST still works.
  </action>
  <verify>
    <automated>pytest tests/test_deployment.py tests/test_api.py -q -k "api_token or batch_job_events"</automated>
  </verify>
  <done>SSE y download protegidos; query token aceptado</done>

</task>

<task type="auto">
  <name>Task 2: UI token settings + apiFetch</name>
  <files>static/index.html, static/css/app.css, static/js/app.js</files>
  <action>

    1. Add collapsible `#api-token-section` (password input, Guardar, Borrar) — copy pattern from glossary/history panels.
    2. localStorage key `md-translate-api-token`; never log token to console.
    3. Helpers: `getApiToken()`, `authHeaders()`, `apiFetch(url, init)` merging Authorization.
    4. Replace all `fetch('/api/...')` with `apiFetch` except `/api/languages` (public).
    5. EventSource: `authEventSourceUrl(path)` appends `?access_token=` when token set (handle existing query with `&`).
    6. On 401 response: showStatus hint to configure token in settings.
  </action>
  <verify>
    <automated>grep -q 'getApiToken' static/js/app.js && grep -q 'apiFetch' static/js/app.js && grep -q 'api-token' static/index.html</automated>
  </verify>
  <done>UI envía Bearer; SSE usa query token</done>

</task>

</tasks>
