---
phase: 06-v1-tech-debt-closure
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - static/index.html
  - static/css/app.css
  - static/js/app.js
autonomous: true
requirements:
  - DEBT-03
must_haves:
  truths:
    - "Tras traducir 2+ idiomas en editor, tabs permiten ver cada traducción"
    - "Descargar cambia filename según idioma activo"
    - "Preview y panel validación siguen al idioma activo"
  artifacts:
    - path: static/js/app.js
      provides: "state.translationResults, renderResultLangTabs, showTranslationForLang"
  key_links:
    - from: translateEditor
      to: state.translationResults
      via: "data.translations map"
    - from: renderResultLangTabs
      to: els.outputMd / preview / validation
      via: "showTranslationForLang(lang)"
---

<objective>
Editor multi-idioma: tabs para preview/output/download por idioma (DEBT-03).

Purpose: Completar UX multi-destino de fase 4 en modo editor (API ya devuelve map).
Output: Tabs UI + state wiring; status message ya indica multi-lang.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-v1-tech-debt-closure/06-CONTEXT.md
@.planning/phases/04-team-scale/04-03-PLAN.md
@static/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup tabs resultado</name>
  <files>static/index.html, static/css/app.css</files>
  <action>
    1. Above `#output-md` (or preview section) add `#result-lang-tabs` container, hidden by default.
    2. CSS: tab pills matching target-lang chips (active state, focus ring).
    3. aria-role tablist / tab / aria-selected for a11y.
  </action>
  <verify>
    <automated>grep -q 'result-lang-tabs' static/index.html</automated>
  </verify>
  <done>DOM tabs listo</done>
</task>

<task type="auto">
  <name>Task 2: State + switcher JS</name>
  <files>static/js/app.js</files>
  <action>
    1. Add `state.translationResults = null`, `state.activeResultLang = null`.
    2. In `translateEditor` (non-review path): if `data.translations`, store full map; else single-lang compat.
    3. `renderResultLangTabs()`: show tabs when Object.keys(translations).length > 1; highlight active.
    4. `showTranslationForLang(lang)`: set outputMd, preview result, validation panel, downloadBlob/Name (`traduccion.{lang}.md`), diff if applicable.
    5. Hide tabs on mode switch / new translate start / review draft path.
    6. Draft/review mode: keep single-lang behavior (no tabs) — out of scope for multi-draft.
  </action>
  <verify>
    <automated>grep -q 'translationResults' static/js/app.js && grep -q 'showTranslationForLang' static/js/app.js</automated>
  </verify>
  <done>Usuario puede alternar idiomas en editor</done>
</task>

</tasks>
