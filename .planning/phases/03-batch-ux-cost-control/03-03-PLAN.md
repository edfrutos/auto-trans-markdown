---
phase: 03-batch-ux-cost-control
plan: 03
type: execute
wave: 2
depends_on:

  - 03-01
  - 03-02

files_modified:

  - static/index.html
  - static/css/app.css
  - static/js/app.js

autonomous: true
requirements:

  - JOB-02
  - JOB-03
  - COST-02

must_haves:
  truths:

    - "Pestaña Lote usa SSE real con barra global y lista por archivo"
    - "Botón Cancelar con confirm() detiene job y ofrece ZIP parcial"
    - "Estimate inline visible en Lote y Archivo antes de Traducir"
  artifacts:
    - path: static/js/app.js
      provides: "translateBatch via jobs, EventSource, fetchEstimate, cancelBatchJob"
    - path: static/index.html
      provides: "batch-progress-section, estimate-batch, estimate-file, btn-cancel-job"
  key_links:
    - from: static/js/app.js
      to: /api/translate/batch/jobs
      via: "fetch + EventSource"
    - from: static/js/app.js
      to: /api/translate/estimate
      via: "fetchEstimate"

---

<objective>
UI: progreso SSE en lote, cancelación, estimación inline (JOB-02, JOB-03 UI, COST-02, D-01–D-05, D-09–D-13, D-14–D-17).

Purpose: Reemplazar spinner falso 30% en lote; visibilidad de coste pre-traducción.
Output: HTML/CSS/JS según 03-UI-SPEC.md.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-batch-ux-cost-control/03-CONTEXT.md
@.planning/phases/03-batch-ux-cost-control/03-UI-SPEC.md
@.planning/phases/03-batch-ux-cost-control/03-RESEARCH.md
@static/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup batch progress + estimate blocks</name>
  <files>static/index.html, static/css/app.css</files>
  <read_first>.planning/phases/03-batch-ux-cost-control/03-UI-SPEC.md, static/index.html, static/css/app.css</read_first>
  <action>

    1. In `#panel-batch` after `#batch-list`, add:
       - `#estimate-batch` (hidden by default) + `#estimate-warn` child for threshold
       - `#batch-progress-section` (hidden): `#batch-progress-bar` track/fill, `#batch-progress-text`, `#batch-file-progress-list` ul, `#btn-cancel-job` button

    2. In `#panel-file` before translate button: `#estimate-file` + `#estimate-warn-file`

    3. CSS in app.css:
       - `.batch-file-pending|active|ok|error|cancelled` icon colors per UI-SPEC
       - `#batch-progress-section` flex-col gap-3; list max-h-48 overflow-y-auto
       - `#btn-cancel-job` danger outline styles
       - Progress bar aria-friendly dimensions (min-height 8px)

    4. Do NOT add estimate to editor panel (D-14).
  </action>
  <verify>
    <automated>grep -q 'batch-progress-section' static/index.html && grep -q 'estimate-batch' static/index.html && grep -q 'estimate-file' static/index.html</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'id="batch-progress-section"' static/index.html
    grep -q 'id="estimate-batch"' static/index.html
    grep -q 'id="estimate-file"' static/index.html
    grep -q 'id="btn-cancel-job"' static/index.html
    grep -q 'batch-progress' static/css/app.css
  </acceptance_criteria>
  <done>DOM y estilos listos para JS</done>

</task>

<task type="auto">
  <name>Task 2: fetchEstimate para lote y archivo</name>
  <files>static/js/app.js</files>
  <read_first>static/js/app.js, .planning/phases/03-batch-ux-cost-control/03-UI-SPEC.md, src/main.py</read_first>
  <action>

    1. Extend `state`: `batchJobId`, `batchJobActive`, `eventSource`, `estimateController` (AbortController).

    2. `async function fetchEstimateBatch()`: if !batchFiles.length hide block; else FormData files + langs → POST /api/translate/estimate → render `#estimate-batch` text: `~${segments} segmentos · ~${characters} chars · ~$${cost.toFixed(2)} (${model})`; show `#estimate-warn` if exceeds_threshold.

    3. `async function fetchEstimateFile()`: when state.selectedFile set, FormData single file → same endpoint → `#estimate-file`.

    4. Debounce 300ms on file selection / batch list change / lang change.

    5. Hook: batch file picker callback and file tab selection call fetchEstimate*.

    6. Editor tab: no estimate calls.
  </action>
  <verify>
    <automated>grep -q 'fetchEstimateBatch' static/js/app.js && grep -q '/api/translate/estimate' static/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'function fetchEstimateBatch' static/js/app.js
    grep -q 'function fetchEstimateFile' static/js/app.js
    grep -q 'estimate-batch' static/js/app.js
    grep -q 'exceeds_threshold' static/js/app.js
  </acceptance_criteria>
  <done>Estimate inline en lote y archivo</done>

</task>

<task type="auto">
  <name>Task 3: translateBatch con SSE + cancel + summary</name>
  <files>static/js/app.js</files>
  <read_first>static/js/app.js, .planning/phases/03-batch-ux-cost-control/03-UI-SPEC.md, .planning/phases/03-batch-ux-cost-control/03-CONTEXT.md</read_first>
  <action>

    1. Rewrite `translateBatch()`:
       - Do NOT use setLoading(true) fake 30% (D-05)
       - Show `#batch-progress-section`, disable Traducir, show Cancel
       - POST /api/translate/batch/jobs with FormData
       - Open EventSource on `/api/translate/batch/jobs/${id}/events`
       - Initialize `#batch-file-progress-list` li per file (pending)
       - Handle events: file_start (active), segment_progress (update bar text + width), file_done (ok), error (error icon), complete (close ES, fetch download URL)

    2. Global bar width: `(completedFiles + done/total of active) / totalFiles * 100`.

    3. `cancelBatchJob()`: if !confirm('¿Cancelar la traducción en curso?') return; DELETE job; close ES; enable download if partial; showStatus cancellation summary; resetBatchJobUI() hiding progress section (D-13).

    4. On complete:
       - Partial: showStatus `${ok}/${total} OK — ${errors} errores...`, warning type if errors
       - Full: success message
       - GET download → state.downloadBlob, show btnDownload

    5. `resetBatchJobUI()`: clear job state, hide progress, re-enable Traducir.

    6. Editor/file modes: keep existing setLoading spinner unchanged (D-03).

    7. Wire `#btn-cancel-job` click to cancelBatchJob.
  </action>
  <verify>
    <automated>grep -q 'EventSource' static/js/app.js && grep -q 'cancelBatchJob' static/js/app.js && grep -q 'batch/jobs' static/js/app.js</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'EventSource' static/js/app.js
    grep -q 'batch/jobs' static/js/app.js
    grep -q 'cancelBatchJob' static/js/app.js
    grep -q 'segment_progress' static/js/app.js
    ! grep -q "progressBar.style.width = '30%'" static/js/app.js || grep -q 'batchJobActive' static/js/app.js
  </acceptance_criteria>
  <done>Flujo lote SSE completo con cancel y resumen</done>

</task>

</tasks>

<verification>

1. Manual UAT: batch 3 files — progress list updates, download works
2. Manual: cancel mid-batch — confirm, partial download
3. Manual: estimate shows on file select and batch file add

</verification>
