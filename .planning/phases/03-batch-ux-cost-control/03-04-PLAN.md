---
phase: 03-batch-ux-cost-control
plan: 04
type: execute
wave: 3
depends_on:

  - 03-01
  - 03-02
  - 03-03

files_modified:

  - tests/test_api.py
  - tests/test_integration.py
  - README.md

autonomous: true
requirements:

  - JOB-01
  - JOB-02
  - JOB-03
  - JOB-04
  - COST-01
  - COST-02

must_haves:
  truths:

    - "Suite pytest cubre estimate, batch zip, jobs y API job endpoints"
    - "README documenta jobs SSE y ESTIMATE_WARN_USD"
  artifacts:
    - path: tests/test_api.py
      provides: "integration tests batch jobs + estimate"
  key_links:
    - from: tests/test_api.py
      to: src/main.py
      via: "TestClient"

---

<objective>
Cerrar cobertura de integración y documentación para Phase 3 (todos JOB-* y COST-*).

Purpose: Verificación automatizada y onboarding de nuevas rutas.
Output: Tests ampliados + README section.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-batch-ux-cost-control/03-VALIDATION.md
@.planning/phases/03-batch-ux-cost-control/03-RESEARCH.md
@tests/test_api.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests API jobs + estimate edge cases</name>
  <files>tests/test_api.py, tests/test_integration.py</files>
  <read_first>tests/test_api.py, tests/conftest.py, src/jobs.py, src/estimate.py</read_first>
  <action>

    1. `test_estimate_json_content`: POST /api/translate/estimate with JSON body sample md → assert segments >= 1, estimated_cost_usd >= 0.

    2. `test_estimate_multipart_files`: upload 2 small .md fixtures → aggregated segments.

    3. `test_batch_job_create_returns_id`: POST /api/translate/batch/jobs with mocked translate_markdown (monkeypatch) returning fast TranslateResult → 200 + job_id.

    4. `test_batch_job_download_after_complete`: poll or await job completion in test; GET download → 200 application/zip.

    5. `test_batch_job_cancel`: start job with slow mock; DELETE; assert cancelled state and zip still downloadable if partial.

    6. `test_batch_zip_errors_json_in_partial`: integration via jobs mock one failure → download zip, assert 'errors.json' in namelist.

    7. Ensure all tests use mocks — no real API keys.
  </action>
  <verify>
    <automated>pytest tests/test_api.py tests/test_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    pytest tests/test_api.py -q -k 'estimate or batch_job' exits 0
    pytest tests/ -q exits 0
  </acceptance_criteria>
  <done>Integración API job + estimate verde</done>

</task>

<task type="auto">
  <name>Task 2: README y .env.example sync</name>
  <files>README.md, .env.example</files>
  <read_first>README.md, .env.example, .planning/phases/03-batch-ux-cost-control/03-CONTEXT.md</read_first>
  <action>

    1. README section «Batch jobs (SSE)» documenting:
       - POST /api/translate/batch/jobs
       - GET .../events (SSE event types)
       - DELETE cancel
       - GET .../download
       - Note: sync POST /api/translate/batch still available

    2. README section «Cost estimate»:
       - POST /api/translate/estimate
       - ESTIMATE_WARN_USD

    3. Verify .env.example has ESTIMATE_WARN_USD (from plan 01).

    4. No secrets in docs.
  </action>
  <verify>
    <automated>grep -q 'batch/jobs' README.md && grep -q 'ESTIMATE_WARN_USD' README.md</automated>
  </verify>
  <acceptance_criteria>
    grep -q '/api/translate/batch/jobs' README.md
    grep -q '/api/translate/estimate' README.md
    grep -q 'ESTIMATE_WARN_USD' .env.example
  </acceptance_criteria>
  <done>Documentación al día</done>

</task>

</tasks>

<verification>

1. `pytest tests/ -q` full green
2. Cross-check REQUIREMENTS JOB-01..04 COST-01..02 covered in test names or manual map

</verification>
