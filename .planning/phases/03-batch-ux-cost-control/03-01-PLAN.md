---
phase: 03-batch-ux-cost-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/estimate.py
  - src/main.py
  - .env.example
  - tests/test_estimate.py
autonomous: true
requirements:
  - COST-01
must_haves:
  truths:
    - "estimate_markdown cuenta segmentos traducibles sin llamar al proveedor"
    - "POST /api/translate/estimate devuelve segments, characters, cost, provider, model, exceeds_threshold"
    - "ESTIMATE_WARN_USD configurable en .env con default 1.0"
  artifacts:
    - path: src/estimate.py
      provides: "estimate_markdown, estimate_files, EstimateResult"
    - path: tests/test_estimate.py
      provides: "unit tests pricing and threshold"
  key_links:
    - from: src/estimate.py
      to: src/parser.py
      via: "segment_markdown + collect_translatable"
---

<objective>
Implementar estimación de coste pre-traducción (COST-01, D-14–D-16).

Purpose: Usuario ve segmentos/chars/coste antes de confirmar lote o archivo.
Output: `src/estimate.py` + `POST /api/translate/estimate` + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-batch-ux-cost-control/03-CONTEXT.md
@.planning/phases/03-batch-ux-cost-control/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: estimate.py con conteo y pricing</name>
  <files>src/estimate.py, tests/test_estimate.py</files>
  <read_first>src/pipeline.py, src/parser.py, src/translator.py, src/memory.py, .planning/phases/03-batch-ux-cost-control/03-RESEARCH.md</read_first>
  <action>
    1. Crear `src/estimate.py` con docstring módulo en español.

    2. `@dataclass EstimateResult`: `segments: int`, `characters: int`, `estimated_cost_usd: float`, `provider: str`, `model: str`, `exceeds_threshold: bool`, `threshold_usd: float`.

    3. `def _threshold_usd() -> float`: leer `os.getenv("ESTIMATE_WARN_USD", "1.0")`, parse float, fallback 1.0 on error.

    4. Pricing constants (documentar en comentario — actualizables):
       - OpenAI: `OPENAI_INPUT_USD_PER_1M = 0.15` default for gpt-4o-mini (use `OPENAI_MODEL` env)
       - DeepL: `DEEPL_USD_PER_MILLION_CHARS = 20.0` (approx free/pro tier — comment only)
       - Cost = `(characters / 4) / 1_000_000 * rate` for OpenAI; `characters / 1_000_000 * rate` for DeepL

    5. `estimate_markdown(content: str, *, target_lang: str, source_lang: str | None = None, use_memory: bool = True) -> EstimateResult`:
       - `segments = collect_translatable(segment_markdown(content))`
       - If use_memory: partition with TranslationMemory.lookup like pipeline; count only miss segments for characters/cost
       - `characters = sum(len(t) for _, t in billable_segments)`
       - `segments = len(billable_segments)` (or total translatable count — use billable/miss count for cost accuracy)
       - Compute cost from `get_provider()` branch
       - `exceeds_threshold = estimated_cost_usd > threshold_usd`

    6. `estimate_files(contents: list[str], **kwargs) -> EstimateResult`: aggregate sum of segments/chars/cost across files.

    7. Tests: empty md → 0 segments; sample with fences → counts only translatable; threshold flag when cost > 1.0 with mock low threshold via monkeypatch.
  </action>
  <verify>
    <automated>pytest tests/test_estimate.py -q</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'def estimate_markdown' src/estimate.py
    grep -q 'class EstimateResult' src/estimate.py
    grep -q 'ESTIMATE_WARN_USD' .env.example
    pytest tests/test_estimate.py -q exits 0
  </acceptance_criteria>
  <done>estimate.py exporta API estable con tests green</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/translate/estimate en main.py</name>
  <files>src/main.py, .env.example</files>
  <read_first>src/estimate.py, src/main.py, .planning/phases/03-batch-ux-cost-control/03-UI-SPEC.md</read_first>
  <action>
    1. Pydantic models: `EstimateRequest` with optional `content: str`; `EstimateResponse` mirroring EstimateResult fields.

    2. `@app.post("/api/translate/estimate", response_model=EstimateResponse)`:
       - Accept JSON `{content, target_lang, source_lang?}` for editor-less estimate
       - Accept multipart `files[]` + `target_lang` + `source_lang` Form (same validation as batch: max 20, .md only, UTF-8 via `_decode_upload`)
       - Validate languages with `_validate_languages_http`
       - Run estimate in `run_in_executor` for file reads/aggregation
       - Return JSON with all COST-01 fields

    3. Add to `.env.example`:
       ```
       # Umbral USD para aviso de coste estimado (COST-02 backend flag)
       ESTIMATE_WARN_USD=1.0
       ```

    4. Extend `tests/test_api.py`: POST estimate with JSON content returns 200 and keys `segments`, `characters`, `estimated_cost_usd`, `exceeds_threshold`.
  </action>
  <verify>
    <automated>pytest tests/test_api.py -q -k estimate</automated>
  </verify>
  <acceptance_criteria>
    grep -q '/api/translate/estimate' src/main.py
    grep -q 'ESTIMATE_WARN_USD' .env.example
    pytest tests/test_api.py -q -k estimate exits 0
  </acceptance_criteria>
  <done>Endpoint estimate operativo con tests API</done>
</task>

</tasks>

<verification>
1. `pytest tests/test_estimate.py tests/test_api.py -q -k estimate`
2. Manual: curl POST with sample markdown returns cost fields
</verification>
