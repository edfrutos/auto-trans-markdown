---
phase: 05-editorial-pro-workflow
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/gitignore_filter.py
  - src/cli.py
  - requirements.txt
  - pyproject.toml
  - tests/test_gitignore_filter.py
  - tests/test_cli.py
autonomous: true
requirements:
  - WATCH-01
  - TREE-01
must_haves:
  truths:
    - "md-translate watch traduce al guardar"
    - "dir --respect-gitignore omite node_modules etc"
---

<objective>
CLI watch + árbol con .gitignore (WATCH-01, TREE-01).
</objective>

<tasks>
<task type="auto">
  <name>Task 1: gitignore_filter.py</name>
  <files>src/gitignore_filter.py, tests/test_gitignore_filter.py</files>
  <action>
    1. load_gitignore_patterns(root), is_ignored(path).
    2. Built-in dirs node_modules, .git, .venv.
    3. Tests patterns básicos.
  </action>
  <verify><automated>pytest tests/test_gitignore_filter.py -q</automated></verify>
</task>
<task type="auto">
  <name>Task 2: watch + dir flag</name>
  <files>src/cli.py, requirements.txt, pyproject.toml, tests/test_cli.py</files>
  <action>
    1. pip dep watchdog>=6.0.
    2. `watch` subcommand debounce 2s.
    3. `--respect-gitignore` on dir/batch (default true for dir).
  </action>
  <verify><automated>pytest tests/test_cli.py -q -k "watch or gitignore"</automated></verify>
</task>
</tasks>
