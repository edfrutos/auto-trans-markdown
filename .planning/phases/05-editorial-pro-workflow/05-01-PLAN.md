---
phase: 05-editorial-pro-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - src/translator.py
  - src/pipeline.py
  - src/main.py
  - .env.example
  - tests/test_translator.py
  - tests/test_pipeline.py

autonomous: true
requirements:

  - FALL-01

must_haves:
  truths:

    - "DeepL failure triggers OpenAI retry when TRANSLATION_FALLBACK=openai"
    - "Sin fallback configurado, error original se propaga"
  artifacts:
    - path: src/translator.py
      provides: "get_fallback_provider, fallback in translate_segments"

---

<objective>
Fallback automático DeepL → OpenAI (FALL-01, NOTEBOOK §13).

Purpose: Resiliencia cuando DeepL falla por cuota o idioma.
Output: Env TRANSLATION_FALLBACK + tests.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: fallback en translator</name>
  <files>src/translator.py, tests/test_translator.py</files>
  <action>

    1. `get_fallback_provider() -> str | None` lee TRANSLATION_FALLBACK.
    2. En translate_segments/deepl batch: on failure, if fallback openai + key, retry chunk with OpenAI.
    3. Log warning con motivo.
    4. Tests mock deepl fail → openai success.
  </action>
  <verify><automated>pytest tests/test_translator.py -q -k fallback</automated></verify>

</task>
<task type="auto">
  <name>Task 2: provider_used en pipeline/API</name>
  <files>src/pipeline.py, src/main.py, .env.example</files>
  <action>

    1. TranslateResult optional provider_used field.
    2. Document TRANSLATION_FALLBACK in .env.example.
  </action>
  <verify><automated>pytest tests/test_pipeline.py -q</automated></verify>

</task>
</tasks>
