---
phase: 07-pdf-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - src/pdf_export.py
  - src/html_export.py
  - pyproject.toml
  - requirements.txt
  - tests/test_pdf_export.py

autonomous: true
requirements:

  - PDF-01
  - PDF-04

must_haves:
  truths:

    - "markdown_to_pdf convierte MD traducido a bytes PDF"
    - "WeasyPrint opcional; is_pdf_available() detecta disponibilidad"
  artifacts:
    - path: src/pdf_export.py
      provides: "markdown_to_pdf, is_pdf_available, PdfExportError"
  key_links:
    - from: src/pdf_export.py
      to: src/html_export.py
      via: "markdown_to_html → WeasyPrint HTML.write_pdf"

---

<objective>
Módulo `pdf_export.py` — MD → PDF vía HTML intermedio (PDF-01, PDF-04).

Purpose: Core reutilizable para CLI y API.
Output: Módulo + extra `[pdf]` + tests con mock.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-pdf-export/07-CONTEXT.md
@.planning/phases/07-pdf-export/07-RESEARCH.md
@src/html_export.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: pdf_export.py</name>
  <files>src/pdf_export.py</files>
  <action>

    1. `is_pdf_available() -> bool` — try import weasyprint.
    2. `markdown_to_pdf(content: str, *, title: str = "Document") -> bytes`.
    3. Usar `markdown_to_html` de html_export; WeasyPrint `HTML(string=html).write_pdf()`.
    4. Si WeasyPrint ausente: raise `RuntimeError` con mensaje pip install weasyprint / `.[pdf]`.
    5. No modificar lógica MD de html_export salvo exportar CSS si hace falta `@page` mínimo para márgenes PDF.
  </action>
  <verify>
    <automated>python -c "from src.pdf_export import is_pdf_available, markdown_to_pdf"</automated>
  </verify>
  <done>API pública pdf_export estable</done>

</task>

<task type="auto">
  <name>Task 2: Optional dependency + tests</name>
  <files>pyproject.toml, requirements.txt, tests/test_pdf_export.py</files>
  <action>

    1. `[project.optional-dependencies] pdf = ["weasyprint>=62"]` en pyproject.toml.
    2. Comentario en requirements.txt: `# weasyprint>=62  # optional: pip install weasyprint`.
    3. Tests: mock `weasyprint.HTML.write_pdf` → bytes `b"%PDF-1.4 mock"`; assert markdown_to_pdf returns PDF bytes.
    4. Test `is_pdf_available` con/without mock import.
    5. Test contenido: output contiene señal de mock invocado con HTML que incluye `<h1>`.
  </action>
  <verify>
    <automated>pytest tests/test_pdf_export.py -q</automated>
  </verify>
  <done>Tests green sin WeasyPrint real en CI</done>

</task>

</tasks>
