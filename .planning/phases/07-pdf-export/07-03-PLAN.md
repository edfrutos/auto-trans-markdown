---
phase: 07-pdf-export
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
files_modified:
  - src/main.py
  - static/index.html
  - static/js/app.js
  - tests/test_api.py
  - README.md
autonomous: true
requirements:
  - PDF-03
  - PDF-04
must_haves:
  truths:
    - "POST /api/export/pdf devuelve application/pdf"
    - "Botón Export PDF en UI junto a Export HTML"
    - "README documenta pip install weasyprint y deps sistema"
  artifacts:
    - path: static/js/app.js
      provides: "exportPdf via apiFetch"
    - path: src/main.py
      provides: "POST /api/export/pdf"
  key_links:
    - from: exportPdf
      to: /api/export/pdf
      via: "JSON content + title → blob download"
---

<objective>
UI Export PDF + API + README (PDF-03, PDF-04).

Purpose: Cerrar milestone v2.0 con UX web y documentación ops.
Output: Endpoint, botón, docs instalación.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-pdf-export/07-CONTEXT.md
@static/js/app.js
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: API POST /api/export/pdf</name>
  <files>src/main.py, tests/test_api.py</files>
  <action>
    1. Pydantic `ExportPdfRequest`: content (str), title (str, default "Document").
    2. Route POST `/api/export/pdf` con Depends(_require_api_token).
    3. Llama markdown_to_pdf; 503 si RuntimeError (WeasyPrint ausente).
    4. Response StreamingResponse o Response bytes, media_type application/pdf, Content-Disposition attachment.
    5. Test mock markdown_to_pdf → 200 + content-type pdf.
  </action>
  <verify>
    <automated>pytest tests/test_api.py -q -k export_pdf</automated>
  </verify>
  <done>API PDF funcional con mock</done>
</task>

<task type="auto">
  <name>Task 2: UI Export PDF</name>
  <files>static/index.html, static/js/app.js</files>
  <action>
    1. Botón `#btn-export-pdf` junto a `#btn-export-html`, hidden por defecto.
    2. `exportPdf()`: toma `els.outputMd.value`, title de `state.downloadName` o active lang tab.
    3. `apiFetch('/api/export/pdf', { method POST, JSON body })` → blob → download `*.pdf`.
    4. Mostrar botón en `onTranslateSuccess` / `showTranslationForLang` igual que HTML.
    5. Error 503: showStatus indicando instalar WeasyPrint en servidor.
  </action>
  <verify>
    <automated>grep -q 'btn-export-pdf' static/index.html && grep -q 'exportPdf' static/js/app.js</automated>
  </verify>
  <done>UI descarga PDF del idioma activo</done>
</task>

<task type="auto">
  <name>Task 3: README + arquitectura</name>
  <files>README.md</files>
  <action>
    1. Sección «Export PDF (opcional)»: `pip install weasyprint` o `pip install -e ".[pdf]"`.
    2. Deps sistema: macOS (brew), Debian/Ubuntu (apt packages), Docker nota (imagen base sin PDF; build opcional).
    3. CLI ejemplo: `md-translate export doc.es.md -o doc.es.pdf --format pdf`.
    4. Web: botón Export PDF requiere WeasyPrint en servidor.
    5. Añadir `src/pdf_export.py` al diagrama arquitectura.
  </action>
  <verify>
    <automated>grep -q 'pdf_export\|WeasyPrint\|--format pdf' README.md</automated>
  </verify>
  <done>Documentación instalación y limitaciones</done>
</task>

</tasks>
