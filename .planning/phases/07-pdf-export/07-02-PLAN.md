---
phase: 07-pdf-export
plan: 02
type: execute
wave: 2
depends_on:

  - 07-01

files_modified:

  - src/cli.py
  - tests/test_cli.py

autonomous: true
requirements:

  - PDF-02

must_haves:
  truths:

    - "md-translate export --format pdf genera .pdf"
    - "default --format html preserva comportamiento actual"
  artifacts:
    - path: src/cli.py
      provides: "export_cmd con --format html|pdf"
  key_links:
    - from: src/cli.py export_cmd
      to: src/pdf_export.py
      via: "markdown_to_pdf cuando format=pdf"

---

<objective>
CLI export PDF (PDF-02).

Purpose: Paridad con export HTML existente.
Output: Flag `--format` + test CLI.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/07-pdf-export/07-RESEARCH.md
@src/cli.py
@tests/test_cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender export_cmd</name>
  <files>src/cli.py</files>
  <action>

    1. Añadir `--format` Typer Option: `html` (default) | `pdf`.
    2. `format=html`: flujo actual `markdown_to_html` → write text.
    3. `format=pdf`: `markdown_to_pdf` → write bytes; capturar RuntimeError → `_exit_config(msg)`.
    4. Help text actualizado; `export --help` lista `--format`.
  </action>
  <verify>
    <automated>md-translate export --help | grep -q format</automated>
  </verify>
  <done>CLI acepta --format pdf</done>

</task>

<task type="auto">
  <name>Task 2: Test CLI export PDF</name>
  <files>tests/test_cli.py</files>
  <action>

    1. `test_export_pdf`: monkeypatch `markdown_to_pdf` retorna `b"%PDF-test"`.
    2. Invocar `export doc.md -o out.pdf --format pdf`.
    3. Assert exit 0, archivo existe, bytes empiezan con `%PDF`.
    4. `test_export_html_still_default`: sin --format → .html como antes.
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q -k export</automated>
  </verify>
  <done>Regresión HTML + PDF mock verdes</done>

</task>

</tasks>
