---
phase: 04-team-scale
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - src/deployment.py
  - src/main.py
  - .env.example
  - tests/test_deployment.py
  - tests/test_api.py
autonomous: true
requirements:
  - SEC-01
  - SEC-02
must_haves:
  truths:
    - "CORS_ORIGINS configurable; default restrictivo localhost:5400"
    - "Upload rechazado si supera MAX_UPLOAD_MB o MAX_BATCH_UPLOAD_MB"
    - "output/ se limpia al arrancar y periódicamente según OUTPUT_TTL_HOURS"
  artifacts:
    - path: src/deployment.py
      provides: "get_cors_origins, check_upload_limits, sweep_output_dir"
  key_links:
    - from: src/main.py
      to: src/deployment.py
      via: "CORSMiddleware origins + startup sweep"
---

<objective>
Hardening de despliegue: CORS, límites upload, TTL output/ (SEC-01, SEC-02, D-16–D-19).

Purpose: Instancia desplegable más segura sin auth obligatorio.
Output: `deployment.py` + hooks en main + tests.
</objective>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Open CORS * in production | high | CORS_ORIGINS env; default localhost |
| Large upload DoS | medium | MAX_UPLOAD_MB + MAX_BATCH_UPLOAD_MB |
| Disk fill via output/ | medium | TTL sweep startup + periodic |
| Unauthorized API use | low | Optional API_TOKEN Bearer gate |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-team-scale/04-CONTEXT.md
@.planning/phases/04-team-scale/04-RESEARCH.md
@src/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: deployment.py</name>
  <files>src/deployment.py, tests/test_deployment.py, .env.example</files>
  <action>
    1. `get_cors_origins()`: parse CORS_ORIGINS comma-separated; default `127.0.0.1:5400,localhost:5400` http origins.
    2. `check_upload_limits(file_size, batch_total_bytes)`: raise ValueError with Spanish message.
    3. `sweep_output_dir(path, ttl_hours) -> int`: delete files older than TTL by mtime.
    4. Optional: `verify_api_token(authorization_header)` for Bearer match when API_TOKEN set.
    5. Document env vars in `.env.example`.
    6. Unit tests for cors parse, upload limits, sweep with tmp files.
  </action>
  <verify>
    <automated>pytest tests/test_deployment.py -q</automated>
  </verify>
  <done>Helpers deployment testeados</done>
</task>

<task type="auto">
  <name>Task 2: Integrar en main.py</name>
  <files>src/main.py, tests/test_api.py</files>
  <action>
    1. Replace `allow_origins=["*"]` with `get_cors_origins()`.
    2. In upload paths (`_decode_batch_uploads`, file routes): accumulate sizes; call check_upload_limits.
    3. `@app.on_event("startup")`: sweep output/; schedule asyncio periodic task (OUTPUT_SWEEP_INTERVAL_HOURS).
    4. If API_TOKEN set: dependency/middleware on `/api/*` routes (except maybe GET /api/languages for health — allow without token OR document token required everywhere).
    5. API tests: oversized upload 400; CORS header present.
  </action>
  <verify>
    <automated>pytest tests/test_deployment.py tests/test_api.py -q -k "upload or cors"</automated>
  </verify>
  <done>SEC-01/02 en runtime</done>
</task>

</tasks>
