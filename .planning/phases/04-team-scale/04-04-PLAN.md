---
phase: 04-team-scale
plan: 04
type: execute
wave: 2
depends_on:

  - 04-01

files_modified:

  - src/cli.py
  - tests/test_cli.py

autonomous: true
requirements:

  - MULTI-01
  - MULTI-02

must_haves:
  truths:

    - "md-translate file -t es,en,fr escribe stem.{lang}.md por idioma"
    - "dir y batch generan múltiples salidas por archivo"
  artifacts:
    - path: src/cli.py
      provides: "_parse_targets, multi-lang file/dir/batch loops"
  key_links:
    - from: src/cli.py
      to: src/pipeline.py
      via: "TranslateOptions per target lang"

---

<objective>
CLI paridad multi-idioma (D-20–D-22).

Purpose: Automatización con mismos nombres que web/ZIP.
Output: `-t es,en,fr` en file|dir|batch + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-team-scale/04-CONTEXT.md
@src/cli.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parser y validación multi-target</name>
  <files>src/cli.py, tests/test_cli.py</files>
  <action>

    1. `_parse_targets(target: str) -> list[str]`: split comma, validate each via `is_valid_target_lang`.
    2. `_build_options_for_lang(lang, ...)` wrapper.
    3. Tests: parse "es,en"; reject "es,xx".
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q -k parse_target</automated>
  </verify>
  <done>Parser CLI multi-idioma</done>

</task>

<task type="auto">
  <name>Task 2: file, dir, batch multi-output</name>
  <files>src/cli.py, tests/test_cli.py</files>
  <action>

    1. `file`: for each lang write `{stem}.{lang}{suffix}`; strict applies per lang.
    2. `dir`: nested loop files × langs → `{stem}.{lang}.md` in mirrored tree.
    3. `batch`: ZIP or output-dir with same naming; validation.json as `{stem}.{lang}.validation.json`.
    4. Integration test with mock translate: 2 langs → 2 output files.
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q -k multi</automated>
  </verify>
  <done>CLI multi-idioma completo</done>

</task>

</tasks>
