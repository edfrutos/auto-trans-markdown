---
phase: 04-team-scale
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - src/target_langs.py
  - src/main.py
  - src/jobs.py
  - src/estimate.py
  - src/batch_zip.py
  - tests/test_target_langs.py
  - tests/test_jobs.py
  - tests/test_api.py

autonomous: true
requirements:

  - MULTI-01
  - MULTI-02

must_haves:
  truths:

    - "API acepta target_lang y target_langs; produce stem.{lang}.md"
    - "Jobs SSE incluyen target_lang en eventos multi-idioma"
    - "Estimate agrega coste/segmentos para K idiomas"
  artifacts:
    - path: src/target_langs.py
      provides: "parse_target_langs, validate_target_langs_http, out_name_for_lang"
  key_links:
    - from: src/jobs.py
      to: src/pipeline.py
      via: "file_then_lang loop translate_markdown"

---

<objective>
Backend multi-destino: parsing, validación, jobs, estimate y ZIP (MULTI-01, MULTI-02, D-05–D-10).

Purpose: Un upload genera N traducciones por idioma destino.
Output: `target_langs.py` + endpoints/jobs/estimate extendidos + tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-team-scale/04-CONTEXT.md
@.planning/phases/04-team-scale/04-RESEARCH.md
@src/jobs.py
@src/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: target_langs.py + tests</name>
  <files>src/target_langs.py, tests/test_target_langs.py</files>
  <action>

    1. `parse_target_langs(target_lang, target_langs) -> list[str]`: merge, strip, dedupe preserve order, max 10.
    2. `validate_target_langs_http(langs)`: each `is_valid_target_lang`; HTTPException 400 Spanish.
    3. `out_name_for_lang(filename, lang, used: set) -> str`: `{stem}.{lang}{suffix}` with collision suffix `_2` like `_unique_zip_name`.
    4. `validation_sidecar_name(filename, lang) -> str`: `{stem}.{lang}.validation.json`.
    5. Tests: single target_lang; list target_langs; dedupe; invalid lang; out_name collision.
  </action>
  <verify>
    <automated>pytest tests/test_target_langs.py -q</automated>
  </verify>
  <done>Helper multi-idioma estable con tests</done>

</task>

<task type="auto">
  <name>Task 2: jobs + estimate + batch_zip multi-lang</name>
  <files>src/jobs.py, src/estimate.py, src/batch_zip.py, tests/test_jobs.py</files>
  <action>

    1. BatchJob: `target_langs: list[str]` replace single target_lang (or keep both; worker uses list).
    2. Worker: file_then_lang — for each file, for each lang in target_langs, translate; emit SSE with `target_lang`, `lang_index`, `total_langs`.
    3. Success entries use `out_name_for_lang`; validation sidecar per D-07.
    4. `estimate_files` / JSON estimate: accept multiple langs via loop aggregate; add `language_count`.
    5. Errors include lang in message when useful: `"doc.md (en): ..."`.
    6. Tests: job 1 file 2 langs → 2 successes in zip; SSE event contains target_lang.
  </action>
  <verify>
    <automated>pytest tests/test_jobs.py -q -k multi</automated>
  </verify>
  <done>Worker y estimate multi-idioma</done>

</task>

<task type="auto">
  <name>Task 3: main.py endpoints multi-target</name>
  <files>src/main.py, tests/test_api.py</files>
  <action>

    1. Helper `_parse_langs_from_form(target_lang, target_langs_form)` for multipart (repeat field `target_langs`).
    2. JSON models: add optional `target_langs: list[str]` to translate/estimate requests.
    3. Apply to: translate text/file, batch sync, batch jobs, estimate.
    4. Sync batch: nested loop; writestr each `stem.{lang}.md`.
    5. Tests: POST translate JSON with target_langs; batch job 2 langs → zip namelist.
  </action>
  <verify>
    <automated>pytest tests/test_api.py -q -k multi</automated>
  </verify>
  <done>API multi-destino operativa</done>

</task>

</tasks>
