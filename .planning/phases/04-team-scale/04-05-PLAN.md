---
phase: 04-team-scale
plan: 05
type: execute
wave: 3
depends_on:

  - 04-01
  - 04-02
  - 04-03
  - 04-04

files_modified:

  - Dockerfile
  - docker-compose.yml
  - .dockerignore
  - README.md
  - tests/test_integration.py

autonomous: true
requirements:

  - DOCKER-01
  - DOCKER-02
  - MULTI-01
  - MULTI-02
  - SEC-01
  - SEC-02

must_haves:
  truths:

    - "docker build produce imagen runnable non-root"
    - "compose monta data/ y output/"
    - "README documenta Docker, CORS, multi-idioma"
  artifacts:
    - path: Dockerfile
      provides: "multi-stage slim image"
    - path: docker-compose.yml
      provides: "volumes + healthcheck port 5400"
  key_links:
    - from: docker-compose.yml
      to: .env.example
      via: "env_file"

---

<objective>
Docker, documentación e integración final (DOCKER-01, DOCKER-02 + traceability).

Purpose: Onboarding en un comando; requisitos Phase 4 verificables.
Output: Dockerfile, compose, README, integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-team-scale/04-CONTEXT.md
@.planning/phases/04-team-scale/04-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile + compose + dockerignore</name>
  <files>Dockerfile, docker-compose.yml, .dockerignore</files>
  <action>

    1. Multi-stage: builder pip install -r requirements.txt; runtime copy app, static, create appuser uid 1000.
    2. `EXPOSE 5400`; ENV PORT=5400 HOST=0.0.0.0.
    3. compose: build, ports 5400:5400, volumes ./data:/app/data, ./output:/app/output, env_file .env.
    4. healthcheck GET /api/languages (install curl in image or use python one-liner).
    5. `.dockerignore`: .venv, .git, output, __pycache__, .planning.
    6. Manual verify: `docker build -t md-translate .` succeeds.
  </action>
  <verify>
    <automated>test -f Dockerfile && test -f docker-compose.yml && grep -q '5400' docker-compose.yml</automated>
  </verify>
  <done>Empaquetado Docker listo</done>

</task>

<task type="auto">
  <name>Task 2: README + integration tests</name>
  <files>README.md, tests/test_integration.py</files>
  <action>

    1. README sections: Multi-destino (UI, API target_langs, CLI -t es,en), Docker quickstart, SEC env vars.
    2. Document LAN vs VPS: CORS_ORIGINS, bind 0.0.0.0 in container.
    3. Integration test: multi-lang API translate returns content; full pytest green.
    4. Map REQUIREMENTS MULTI/DOCKER/SEC to test names in comment block.
  </action>
  <verify>
    <automated>pytest tests/ -q && grep -q 'docker compose' README.md && grep -q 'target_langs' README.md</automated>
  </verify>
  <done>Docs y suite verde</done>

</task>

</tasks>
