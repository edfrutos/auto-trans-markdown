---
phase: 04-team-scale
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - static/index.html
  - static/css/app.css
  - static/js/app.js
autonomous: true
requirements:
  - MULTI-01
must_haves:
  truths:
    - "Usuario añade/quita idiomas destino con chips"
    - "Estimate muestra total K idiomas"
    - "Progreso lote muestra sub-entradas por idioma"
  artifacts:
    - path: static/js/app.js
      provides: "state.targetLangs, renderTargetChips, nested SSE progress"
  key_links:
    - from: static/js/app.js
      to: /api/translate/batch/jobs
      via: "target_langs repeated in FormData"
---

<objective>
UI multi-destino: chips, estimate agregado, progreso anidado (MULTI-01, D-01–D-04).

Purpose: Paridad visual con decisiones CONTEXT en editor, archivo y lote.
Output: HTML/CSS/JS actualizados.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-team-scale/04-CONTEXT.md
@.planning/phases/04-team-scale/04-RESEARCH.md
@static/js/app.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup chips destino</name>
  <files>static/index.html, static/css/app.css</files>
  <action>
    1. Below `#target-lang` select add `#target-lang-chips` container.
    2. CSS: chip pill, remove button, min one lang required hint.
    3. Keep select as "Añadir idioma" picker (change adds chip if not present).
  </action>
  <verify>
    <automated>grep -q 'target-lang-chips' static/index.html</automated>
  </verify>
  <done>DOM chips listo</done>
</task>

<task type="auto">
  <name>Task 2: JS chips + API payload</name>
  <files>static/js/app.js</files>
  <action>
    1. `state.targetLangs = []`; init from first loaded language option.
    2. `renderTargetChips()`, add/remove handlers; prevent duplicate langs.
    3. Update all FormData/JSON calls: append each `target_langs`; keep `target_lang` as first for compat.
    4. `fetchEstimate*`: show `(K idiomas)` in aggregate line.
    5. Editor translate: loop langs client-side OR single request if API supports multi JSON — prefer one API call with target_langs.
  </action>
  <verify>
    <automated>grep -q 'targetLangs' static/js/app.js && grep -q 'target_langs' static/js/app.js</automated>
  </verify>
  <done>Payload multi-idioma en UI</done>
</task>

<task type="auto">
  <name>Task 3: Progreso SSE anidado</name>
  <files>static/js/app.js, static/css/app.css</files>
  <action>
    1. On batch start: build file list with nested lang sub-items (pending).
    2. Handle SSE `target_lang` on file_start/file_done/segment_progress/error.
    3. Update sub-item status classes batch-file-* per UI-SPEC phase 3 pattern.
    4. Global bar: weight = files × langs completed fraction.
  </action>
  <verify>
    <automated>grep -q 'target_lang' static/js/app.js && grep -q 'data-lang' static/js/app.js</automated>
  </verify>
  <done>Progreso multi-idioma visible</done>
</task>

</tasks>
