---
phase: 02-trust-qa
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/pipeline.py
  - src/main.py
  - tests/test_pipeline.py
  - tests/test_api.py
autonomous: true
requirements:
  - VAL-02
must_haves:
  truths:
    - "translate_markdown devuelve validation en TranslateResult tras reassemble"
    - "POST /api/translate incluye validation en JSON response"
    - "POST /api/translate/file expone validation vía header X-Validation-Report"
    - "ZIP de lote incluye {stem}.validation.json por cada archivo traducido"
    - "API nunca bloquea por validación error — solo informa (D-01)"
  artifacts:
    - path: src/pipeline.py
      provides: "TranslateResult.validation + hook post-reassemble"
      contains: "validate_translation"
    - path: src/main.py
      provides: "ValidationReportModel en TranslateResponse + batch JSON + file header"
      contains: "ValidationReportModel"
  key_links:
    - from: src/pipeline.py
      to: src/validator.py
      via: "validate_translation(content, output)"
      pattern: "validate_translation"
    - from: src/main.py
      to: src/pipeline.py
      via: "_run_translate maps validation to response"
      pattern: "validation"
    - from: translate_batch
      to: zipfile
      via: "writestr validation.json"
      pattern: "validation\\.json"
---

<objective>
Integrar validador en pipeline y exponer informe en API JSON, header de archivo y ZIP de lote (VAL-02, D-01, D-06, D-07).

Purpose: Usuario y herramientas reciben informe de confianza sin bloquear flujo web.
Output: TranslateResult.validation; TranslateResponse extendido; batch con `.validation.json`; file con header.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-trust-qa/02-CONTEXT.md
@.planning/phases/02-trust-qa/02-RESEARCH.md
@src/pipeline.py
@src/main.py
@src/validator.py
@tests/test_pipeline.py
@tests/test_api.py

<interfaces>
From src/validator.py (plan 02-01):
```python
@dataclass
class ValidationReport:
    overall: str  # pass | warn | error
    checks: list[ValidationCheck]

def validate_translation(original: str, translated: str) -> ValidationReport: ...
def validation_to_dict(report: ValidationReport) -> dict: ...
```

From src/pipeline.py (existing):
```python
@dataclass
class TranslateResult:
    content: str
    segments_total: int
    segments_translated: int
    cache_hits: int = 0
    cache_misses: int = 0
    dry_run_segments: list[tuple[int, str]] | None = None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pipeline — validation en TranslateResult</name>
  <files>src/pipeline.py, tests/test_pipeline.py</files>
  <read_first>src/pipeline.py, src/validator.py, tests/test_pipeline.py, tests/conftest.py</read_first>
  <behavior>
    - Test: translate_markdown con mock devuelve result.validation no None
    - Test: dry_run=True → validation None (sin traducción real)
    - Test: validation.overall coherente cuando output preserva fences del input
  </behavior>
  <action>
    Per D-07, RESEARCH.md:

    1. Importar `validate_translation` desde `src.validator`.

    2. Extender `TranslateResult` con `validation: ValidationReport | None = None`.

    3. Tras `output = reassemble(...)` y antes del return:
       ```python
       validation = validate_translation(content, output)
       ```

    4. Incluir `validation=validation` en TranslateResult de flujo normal.

    5. dry_run early return: `validation=None`.

    6. NO añadir `strict` al pipeline en este plan — reservado plan 02-04 CLI.

    7. Actualizar tests/test_pipeline.py con asserts sobre validation populated.
  </action>
  <verify>
    <automated>pytest tests/test_pipeline.py -q</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'validation' src/pipeline.py
    grep -q 'validate_translation' src/pipeline.py
    pytest tests/test_pipeline.py -q exits 0
  </acceptance_criteria>
  <done>Pipeline siempre valida pares original/traducido en flujo no dry-run</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API — TranslateResponse, file header, batch validation.json</name>
  <files>src/main.py, tests/test_api.py</files>
  <read_first>src/main.py, tests/test_api.py, tests/conftest.py, .planning/phases/02-trust-qa/02-RESEARCH.md</read_first>
  <behavior>
    - Test POST /api/translate: response JSON incluye validation.overall y validation.checks[]
    - Test POST /api/translate/batch mock: ZIP contiene foo.es.md y foo.validation.json
    - Test: validation error no cambia status 200 en translate (D-01)
  </behavior>
  <action>
    Per D-01, D-06, VAL-02:

    1. Añadir modelos Pydantic:
       ```python
       class ValidationCheckModel(BaseModel):
           id: str
           status: str
           message: str
           expected: int | None = None
           actual: int | None = None

       class ValidationReportModel(BaseModel):
           overall: str
           checks: list[ValidationCheckModel]
       ```

    2. Extender `TranslateResponse` con `validation: ValidationReportModel | None = None`.

    3. Helper `_validation_model(result: TranslateResult) -> ValidationReportModel | None` — mapea dataclass → Pydantic vía validation_to_dict.

    4. Refactor `_run_translate` → retornar `TranslateResult` (no TranslateResponse); mappers en cada endpoint construyen response.

    5. `translate_text`: incluir validation en TranslateResponse.

    6. `translate_file`: mantener FileResponse body markdown; añadir header:
       `X-Validation-Report: json.dumps(validation_to_dict(...))` cuando validation presente (UTF-8 safe, sin newlines).

    7. `translate_batch`: por cada archivo traducido:
       - `zf.writestr(out_name, content)`
       - `validation_name = f"{Path(out_name).stem}.validation.json"` (mismo stem que .md traducido)
       - `zf.writestr(validation_name, json.dumps(validation_to_dict(...), ensure_ascii=False, indent=2))`

    8. Actualizar tests/test_api.py — mock translate_markdown retorna TranslateResult con validation mock.

    9. Verificar suite: `pytest tests/test_api.py tests/test_integration.py -q`
  </action>
  <verify>
    <automated>pytest tests/test_api.py tests/test_integration.py -q</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'ValidationReportModel' src/main.py
    grep -q 'X-Validation-Report' src/main.py
    grep -q 'validation.json' src/main.py
    pytest tests/test_api.py -q exits 0
  </acceptance_criteria>
  <done>API expone validation en JSON, header file y ZIP batch; sin bloqueo HTTP</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Markdown upload; validation report en respuesta |
| API→ZIP | validation.json con metadatos estructura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | I | X-Validation-Report header | accept | Metadatos no sensibles; no incluir API keys |
| T-02-03-02 | D | FileResponse sin bloqueo | mitigate | D-01: warnings/errors no bloquean descarga web |
| T-02-03-03 | E | ZIP bomb validation.json | accept | Tamaño acotado por límite 20 archivos existente |
</threat_model>

<verification>
- `pytest tests/test_pipeline.py tests/test_api.py tests/test_integration.py -q`
- grep ValidationReportModel y validation.json en main.py
</verification>

<success_criteria>
- VAL-02 (backend): Informe visible en API translate, header file, validation.json en ZIP lote
- Pipeline ejecuta validación post-traducción automáticamente
</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-qa/02-03-SUMMARY.md`
</output>
