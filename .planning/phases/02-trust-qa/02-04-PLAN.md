---
phase: 02-trust-qa
plan: 04
type: execute
wave: 3
depends_on:
  - "02-03"
files_modified:
  - src/cli.py
  - tests/test_cli.py
autonomous: true
requirements:
  - VAL-03
must_haves:
  truths:
    - "md-translate file --strict no escribe salida si validation.overall == error"
    - "md-translate dir --strict omite escritura en archivos con error de validación"
    - "md-translate batch --strict no genera ZIP/dir con archivos fallidos"
    - "Warnings no bloquean exportación (D-01/D-02)"
    - "Exit code 1 en fallo strict; 0 en pass/warn"
  artifacts:
    - path: src/cli.py
      provides: "--strict en file|dir|batch"
      contains: "--strict"
    - path: tests/test_cli.py
      provides: "Tests exit code y no-write strict"
  key_links:
    - from: src/cli.py
      to: TranslateResult.validation
      via: "check after translate_markdown"
      pattern: "validation\\.overall"
    - from: file_cmd
      to: out_path.write_text
      via: "guard strict before write"
      pattern: "strict"
---

<objective>
Añadir flag `--strict` a comandos CLI file/dir/batch que bloquea exportación si validación falla (VAL-03, D-02).

Purpose: Automatización CI/scripting puede rechazar traducciones estructuralmente rotas.
Output: `--strict` en Typer; exit 1 sin escribir salida cuando overall error.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-trust-qa/02-CONTEXT.md
@.planning/phases/02-trust-qa/02-RESEARCH.md
@src/cli.py
@tests/test_cli.py
@src/pipeline.py

<interfaces>
From src/pipeline.py (plan 02-03):
```python
@dataclass
class TranslateResult:
    content: str
    validation: ValidationReport | None = None
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helper _strict_blocked y flag --strict en file</name>
  <files>src/cli.py, tests/test_cli.py</files>
  <read_first>src/cli.py, tests/test_cli.py, .planning/phases/02-trust-qa/02-CONTEXT.md</read_first>
  <behavior>
    - Test: file --strict con mock validation.overall error → exit 1, archivo salida NO creado
    - Test: file --strict con validation.overall warn → exit 0, archivo escrito
    - Test: file sin --strict con validation error → exit 0, archivo escrito (D-01)
  </behavior>
  <action>
    Per D-01, D-02, VAL-03:

    1. Helper:
       ```python
       def _strict_blocked(result: TranslateResult, strict: bool) -> bool:
           if not strict or not result.validation:
               return False
           return result.validation.overall == "error"
       ```

    2. Helper `_fail_strict(filename: str | None = None) -> None`:
       - Mensaje español: `"Validación fallida — salida no escrita"` (+ nombre archivo si aplica)
       - `typer.secho(..., fg=typer.colors.RED, err=True)`
       - `raise typer.Exit(code=1)`

    3. Añadir a `file_cmd`:
       ```python
       strict: bool = typer.Option(False, "--strict", help="No escribir salida si validación falla")
       ```

    4. Tras `_translate_content`, antes de `write_text`:
       ```python
       if _strict_blocked(result, strict):
           _fail_strict(str(input_path))
       ```

    5. dry_run + strict: ignorar strict (no hay salida que bloquear).

    6. Tests con monkeypatch `translate_markdown` retornando ValidationReport mock.
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q -k strict</automated>
  </verify>
  <acceptance_criteria>
    grep -q '\-\-strict' src/cli.py
    grep -q '_strict_blocked' src/cli.py
    pytest tests/test_cli.py -q -k strict exits 0
  </acceptance_criteria>
  <done>file --strict bloquea solo en error; warn permite export</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: --strict en dir y batch</name>
  <files>src/cli.py, tests/test_cli.py</files>
  <read_first>src/cli.py, tests/test_cli.py</read_first>
  <behavior>
    - Test: dir --strict — archivo con error no escrito; otros archivos OK siguen escribiéndose; exit 1 si algún fallo
    - Test: batch --strict — ZIP no contiene entry para archivo fallido; exit 1
    - Test: batch --strict con todos pass → ZIP completo exit 0
  </behavior>
  <action>
    1. Añadir `strict: bool = typer.Option(False, "--strict", ...)` a `dir_cmd` y `batch_cmd`.

    2. `dir_cmd`: en loop por archivo, tras translate:
       - Si `_strict_blocked(result, strict)`: incrementar `errors`, log `"✗ {rel}: validación fallida"`, **no** write_text, continue
       - Al final si errors > 0: raise typer.Exit(1)

    3. `batch_cmd`:
       - ZIP mode: solo `writestr` si not _strict_blocked
       - output_dir mode: mismo guard antes de write_text
       - Contabilizar errors y exit 1 si algún strict block

    4. Mensaje stderr claro por archivo fallido en modo dir/batch.

    5. Ampliar tests/test_cli.py con fixtures temp dir y mock pipeline.

    6. Suite completa CLI: `pytest tests/test_cli.py -q`
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    grep -c '\-\-strict' src/cli.py | grep -q '[3-9]'
    pytest tests/test_cli.py -q exits 0
  </acceptance_criteria>
  <done>--strict disponible en file, dir, batch con semántica consistente</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI→filesystem | Escritura archivos traducidos |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | D | --strict bypass | mitigate | Check validation.overall antes de write; test coverage |
| T-02-04-02 | E | Partial dir write | accept | dir strict: archivos OK escritos, fallidos omitidos + exit 1 — documentado |
| T-02-04-03 | I | Strict message leak | accept | Mensajes genéricos; sin contenido documento |
</threat_model>

<verification>
- `pytest tests/test_cli.py -q` verde
- Manual: `md-translate file --strict` con fixture que rompe fences → no output file
</verification>

<success_criteria>
- VAL-03: --strict bloquea export si validation overall error
- Warnings no bloquean; exit codes documentados
</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-qa/02-04-SUMMARY.md`
</output>
