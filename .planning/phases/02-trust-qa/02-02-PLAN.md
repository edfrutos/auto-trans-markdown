---
phase: 02-trust-qa
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:

  - src/parser.py
  - tests/test_parser.py

autonomous: true
requirements:

  - PARS-01
  - PARS-02
  - FM-01
  - FM-02

must_haves:
  truths:

    - "Comentarios # en fences python son traducibles; código y shebang protegidos"
    - "Comentarios // en javascript/typescript son traducibles"
    - "Comentarios HTML <!-- --> en fences html/xml son traducibles"
    - "Frontmatter: title/description/summary/tags/categories/keywords traducibles; slug/date/layout/id/author protegidos"
    - "YAML frontmatter inválido permanece bloque PROTECTED íntegro"
  artifacts:
    - path: src/parser.py
      provides: "Registries comentarios + segmentación frontmatter selectiva"
      contains: "FM_TRANSLATABLE_KEYS"
    - path: tests/test_parser.py
      provides: "Tests por lenguaje y frontmatter"
  key_links:
    - from: src/parser.py
      to: yaml.safe_load
      via: "parse frontmatter inner block"
      pattern: "yaml\\.safe_load"
    - from: segment_markdown
      to: _append_comment_line
      via: "fence lang registry"
      pattern: "_append_comment_line|HASH_COMMENT_LANGS"

---

<objective>
Extender parser para comentarios traducibles en python/js/ts/html/xml y frontmatter YAML selectivo (PARS-01, PARS-02, FM-01, FM-02, D-14…D-20).

Purpose: Traducir texto útil en fences y metadatos humanos sin romper claves técnicas.
Output: Registries de lenguaje, helpers comentario unificados, frontmatter whitelist; tests ampliados.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-trust-qa/02-CONTEXT.md
@.planning/phases/02-trust-qa/02-RESEARCH.md
@src/parser.py
@tests/test_parser.py

<interfaces>
From src/parser.py (existing):
```python
class SegmentKind(str, Enum):
    PROTECTED = "protected"
    TRANSLATABLE = "translatable"

@dataclass
class Segment:
    kind: SegmentKind
    text: str
    index: int

def segment_markdown(content: str) -> list[Segment]: ...
def reassemble(segments: list[Segment], translations: dict[int, str]) -> str: ...
def collect_translatable(segments: list[Segment]) -> list[tuple[int, str]]: ...
```
Shell pattern reference: `_is_shell_fence`, `_append_shell_line`, `SHELL_COMMENT`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry comentarios por lenguaje en fences</name>
  <files>src/parser.py, tests/test_parser.py</files>
  <read_first>src/parser.py, .planning/phases/02-trust-qa/02-RESEARCH.md, tests/test_parser.py</read_first>
  <behavior>

    - Test python: `# comment` traducible; `print("x")` protegido; shebang `#!/usr/bin/env python` línea completa protegida
    - Test javascript: `// comment` traducible; código sin prefijo // protegido
    - Test typescript/js alias: fence ` ```ts ` y ` ```js ` funcionan
    - Test html/xml: `<!-- comment -->` traducible en una línea
    - Test: reassemble traduce cuerpo comentario preservando delimitadores
    - Test: tests bash existentes siguen pasando (regresión)
  </behavior>
  <action>
    Per D-18, D-19, D-20, RESEARCH.md:

    1. Añadir constantes módulo:
       ```python
       HASH_COMMENT_LANGS = frozenset({"python"})
       SLASH_COMMENT_LANGS = frozenset({"javascript", "typescript", "js", "ts"})
       HTML_COMMENT_LANGS = frozenset({"html", "xml"})
       SHELL_LANGS  # existente
       HASH_COMMENT = re.compile(r"^(\s*#\s?)(.*?)(\n?)$", re.DOTALL)  # reutilizar SHELL_COMMENT
       SLASH_COMMENT = re.compile(r"^(\s*//\s?)(.*?)(\n?)$", re.DOTALL)
       HTML_COMMENT = re.compile(r"(^[\s]*)(<!--)(.*?)(-->)([\s]*)", re.DOTALL)
       ```

    2. `_fence_lang(info: str) -> str | None` — primer token lower del info string.

    3. `_comment_kind(lang: str) -> str | None` — retorna `"hash"|"slash"|"html"|"shell"|None`.

    4. `_append_comment_line(segments, line, idx, kind) -> int` — unificar shell/python (#), js/ts (//), html (<!-- -->). Shebang `#!` → PROTECTED línea entera sin traducir.

    5. Reemplazar `is_shell = _is_shell_fence(fence_info)` por `comment_kind = _comment_kind(_fence_lang(fence_info) or "")` en loop fence; inner lines usan `_append_comment_line` cuando kind no None, else PROTECTED.

    6. Mantener `_is_shell_fence` como wrapper o eliminar si redundante — preferir un solo path.

    7. Actualizar docstring `segment_markdown` listando lenguajes soportados.
  </action>
  <verify>
    <automated>pytest tests/test_parser.py -q</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'HASH_COMMENT_LANGS' src/parser.py
    grep -q 'SLASH_COMMENT_LANGS' src/parser.py
    grep -q 'HTML_COMMENT_LANGS' src/parser.py
    pytest tests/test_parser.py -q exits 0
  </acceptance_criteria>
  <done>Comentarios traducibles en python, js/ts, html/xml; regresión bash verde</done>

</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontmatter YAML whitelist selectivo</name>
  <files>src/parser.py, tests/test_parser.py</files>
  <read_first>.planning/phases/02-trust-qa/02-CONTEXT.md, src/parser.py</read_first>
  <behavior>

    - Test: `title: Hello` → segmento TRANSLATABLE "Hello"; `slug: my-post` → PROTECTED
    - Test: `tags: [one, two]` — strings en lista traducibles; `date: 2024-01-01` nunca traducible
    - Test: `layout: docs`, `id: 42`, `author: name` protegidos (D-15)
    - Test: YAML inválido en frontmatter → bloque --- entero PROTECTED (D-17)
    - Test: reassemble con traducción de title reconstruye frontmatter válido con slug intacto
  </behavior>
  <action>
    Per D-14, D-15, D-16, D-17, FM-01, FM-02:

    1. `import yaml` (PyYAML ya en requirements.txt).

    2. Constantes:
       ```python
       FM_TRANSLATABLE_KEYS = frozenset({
           "title", "description", "summary", "tags", "categories", "keywords"
       })
       FM_PROTECTED_KEYS = frozenset({"date", "slug", "id", "layout", "author"})
       ```

    3. Reemplazar bloque frontmatter actual (PROTECTED íntegro) por:
       - Detectar `---` … `---` como ahora
       - Extraer inner YAML (sin delimiters)
       - `yaml.safe_load(inner)` — on `yaml.YAMLError`: fallback PROTECTED bloque completo (comportamiento D-17)
       - Si dict válido: `_segment_frontmatter(block, data, segments, idx) -> int` emite:
         - Delimiters `---\n` PROTECTED
         - Por cada clave whitelist con valor string: prefix key PROTECTED + valor TRANSLATABLE
         - Listas: cada string item traducible; números/bools PROTECTED
         - Claves no whitelist / protected / URLs / números: PROTECTED inline preservando YAML literal
       - Cerrar `---\n` PROTECTED

    4. Estrategia reconstrucción: segmentar prefix `key: ` como PROTECTED + value TRANSLATABLE; `reassemble` existente preserva estructura. Para listas `- item` usar patrón similar.

    5. Aceptar reorden menor de claves en round-trip v1 (documentar en test); claves técnicas nunca traducidas.

    6. NO traducir valores que parezcan URL (`http://`, `https://`) aunque estén bajo key whitelist.
  </action>
  <verify>
    <automated>pytest tests/test_parser.py -q</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'FM_TRANSLATABLE_KEYS' src/parser.py
    grep -q 'yaml.safe_load' src/parser.py
    pytest tests/test_parser.py -q -k 'frontmatter or python or javascript or html' exits 0
  </acceptance_criteria>
  <done>Frontmatter selectivo funcional; claves técnicas intactas; YAML roto protegido</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary    | Description                       |
| ----------- | --------------------------------- |
| parser→YAML | Frontmatter de documentos usuario |

## STRIDE Threat Register

| Threat ID   | Category   | Component       | Disposition   | Mitigation Plan                                                      |
| ----------- | ---------- | --------------- | ------------- | -------------------------------------------------------------------- |
| T-02-02-01  | T          | yaml.safe_load  | mitigate      | safe_load only; bloque inválido → PROTECTED entero                   |
| T-02-02-02  | I          | FM whitelist    | mitigate      | FM_PROTECTED_KEYS hardcoded; URLs no traducidas                      |
| T-02-02-03  | D          | Comment parsing | accept        | Edge cases (# in strings) cubiertos por regex `^\s*#` — prudencia v1 |
</threat_model>

<verification>

- `pytest tests/test_parser.py -q` — suite parser verde incl. tests existentes
- grep confirma registries y FM_TRANSLATABLE_KEYS

</verification>

<success_criteria>

- PARS-01/02: Comentarios traducibles en python, js/ts, html/xml
- FM-01/02: Whitelist traducida; slug/date/layout/id/author protegidos

</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-qa/02-02-SUMMARY.md`
</output>
