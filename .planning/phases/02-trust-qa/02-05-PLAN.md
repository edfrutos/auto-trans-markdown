---
phase: 02-trust-qa
plan: 05
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - static/index.html
  - static/css/app.css
  - static/js/app.js
autonomous: false
requirements:
  - PREV-01
  - PREV-02
  - VAL-02
must_haves:
  truths:
    - "Usuario ve preview HTML Original | Traducido bajo textareas tras traducir o cargar ejemplo"
    - "Preview usa marked + DOMPurify — nunca innerHTML sin sanitizar"
    - "Panel Validación colapsable muestra resumen y lista checks tras traducir"
    - "Descarga no se bloquea por errores de validación en UI (D-01)"
    - "Layout preview apilado en móvil, lado a lado en desktop (D-10)"
  artifacts:
    - path: static/index.html
      provides: "CDN marked/DOMPurify + preview row + validation section"
      contains: "validation-section"
    - path: static/css/app.css
      provides: ".prose-preview dark mode"
      contains: "prose-preview"
    - path: static/js/app.js
      provides: "renderPreview, renderValidationPanel"
      contains: "renderPreview"
  key_links:
    - from: static/js/app.js
      to: marked + DOMPurify
      via: "renderPreview sanitization"
      pattern: "DOMPurify\\.sanitize"
    - from: translateEditor
      to: data.validation
      via: "renderValidationPanel after success"
      pattern: "renderValidationPanel"
    - from: translateFile
      to: X-Validation-Report header
      via: "parse JSON header"
      pattern: "X-Validation-Report"
---

<objective>
Implementar preview Markdown sanitizada dual y panel Validación colapsable en UI web (PREV-01, PREV-02, VAL-02 UI, D-05, D-08…D-13).

Purpose: Usuario revisa visualmente traducción y ve informe de confianza sin bloquear export.
Output: CDN scripts, preview panels, validation panel; wired en editor y file tab.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-trust-qa/02-UI-SPEC.md
@.planning/phases/02-trust-qa/02-CONTEXT.md
@static/index.html
@static/css/app.css
@static/js/app.js
@.planning/phases/01-production-table-stakes/01-04-SUMMARY.md

<interfaces>
API validation contract (plan 02-03):
```json
{
  "validation": {
    "overall": "pass",
    "checks": [{ "id": "fences", "status": "pass", "message": "..." }]
  }
}
```
File endpoint: header `X-Validation-Report` con mismo JSON stringificado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Markup CDN, preview row y validation section</name>
  <files>static/index.html, static/css/app.css</files>
  <read_first>.planning/phases/02-trust-qa/02-UI-SPEC.md, static/index.html, static/css/app.css</read_first>
  <action>
    Per 02-UI-SPEC.md, D-08, D-10, D-11, D-12:

    1. En `<head>` o antes de `app.js`, añadir CDN (pinned):
       ```html
       <script src="https://cdn.jsdelivr.net/npm/marked@12.0.2/marked.min.js"></script>
       <script src="https://cdn.jsdelivr.net/npm/dompurify@3.2.4/dist/purify.min.js"></script>
       ```

    2. En panel Editor (tras textareas `#source-md` / `#result-md` o equivalentes):
       - `#preview-row` grid `grid-cols-1 md:grid-cols-2 gap-4 mt-4`
       - `#preview-source.prose-preview` con label «Vista previa — Original»
       - `#preview-result.prose-preview` con label «Vista previa — Traducido»

    3. Insertar `<section id="validation-section" class="hidden mb-6">` bajo área resultado (patrón `#glossary-section`):
       - Toggle `#validation-toggle` aria-expanded/controls
       - Panel `#validation-panel` collapsed default
       - `#validation-summary`, `#validation-checks` ul

    4. CSS `.prose-preview` en app.css:
       - Tipografía prose con variables `--ink`, `--ink-muted`, `--primary`
       - `pre`/`code`: fondos teal light + dark variants
       - `max-h-96 overflow-y-auto`, bordes `border-teal-100`
       - Reuse `.glossary-chevron` para validation toggle

    5. NO añadir toggle modo estricto web (deferred).
  </action>
  <verify>
    <automated>grep -q 'validation-section' static/index.html && grep -q 'preview-row' static/index.html && grep -q 'marked@12' static/index.html && grep -q 'dompurify@3' static/index.html && grep -q 'prose-preview' static/css/app.css</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'validation-section' static/index.html
    grep -q 'preview-source' static/index.html
    grep -q 'prose-preview' static/css/app.css
    grep -q 'dompurify' static/index.html
  </acceptance_criteria>
  <done>HTML/CSS estructura preview + validation presentes; CDN cargados antes app.js</done>
</task>

<task type="auto">
  <name>Task 2: renderPreview + renderValidationPanel + wiring</name>
  <files>static/js/app.js</files>
  <read_first>static/js/app.js, .planning/phases/02-trust-qa/02-UI-SPEC.md, .planning/phases/02-trust-qa/02-CONTEXT.md</read_first>
  <action>
    Per D-09, D-13, PREV-02, VAL-02, 02-UI-SPEC:

    1. Extender `els` con: previewSource, previewResult, validationSection, validationToggle, validationPanel, validationSummary, validationChecks.

    2. `renderPreview(markdown, el)`:
       ```javascript
       if (!el || typeof marked === 'undefined' || typeof DOMPurify === 'undefined') {
         if (el) el.textContent = 'Vista previa no disponible';
         return;
       }
       const raw = marked.parse(markdown || '', { gfm: true, breaks: false });
       el.innerHTML = DOMPurify.sanitize(raw, {
         USE_PROFILES: { html: true },
         FORBID_TAGS: ['script', 'iframe'],
       });
       ```
       NUNCA asignar `raw` sin sanitizar.

    3. `renderValidationPanel(validation)`:
       - Si !validation: hide `#validation-section`
       - Contar pass/warn/error; summary «{n} correctos · {n} avisos · {n} errores»
       - Render lista checks con iconos ✓/⚠/✗ y colores teal/amber/red
       - Labels español: Fences, Enlaces, Imágenes, Código inline, Encabezados
       - Show section; NO deshabilitar btnDownload en error (D-01)

    4. Wire validation toggle expand/collapse (copiar patrón glossary).

    5. `translateEditor` success: `renderPreview(source, previewSource)`, `renderPreview(data.content, previewResult)`, `renderValidationPanel(data.validation)`.

    6. `translateFile` success:
       - Parse header `X-Validation-Report` JSON → renderValidationPanel
       - renderPreview desde blob.text() en previewResult; previewSource desde state o re-read file if available
       - showStatus incluye hint si validation errors

    7. `translateBatch` success: showStatus `'Lote completado — validation.json incluido en ZIP'` (no panel per-file).

    8. Cargar SAMPLE_MD / init: renderPreview sample en previewSource only.

    9. NO bind keyup/input en textareas para preview (D-09).

    10. state.lastValidation opcional para persist entre tabs.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const js=fs.readFileSync('static/js/app.js','utf8'); ['renderPreview','renderValidationPanel','DOMPurify.sanitize','X-Validation-Report','validation-section'].every(s=>js.includes(s)) || process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    grep -q 'renderPreview' static/js/app.js
    grep -q 'DOMPurify.sanitize' static/js/app.js
    grep -q 'renderValidationPanel' static/js/app.js
    grep -q 'X-Validation-Report' static/js/app.js
  </acceptance_criteria>
  <done>Preview sanitizada y panel validación funcionan en editor y file; batch muestra mensaje ZIP</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificación visual preview XSS y validación</name>
  <files>static/index.html, static/js/app.js</files>
  <action>
    Verificación manual post-automatización. No modificar código salvo issues reportados por el usuario.
  </action>
  <what-built>Preview dual marked+DOMPurify; panel Validación; integración API</what-built>
  <how-to-verify>
    1. Arrancar servidor: `python -m src.main` (con API key configurada)
    2. Abrir http://127.0.0.1:8000 — pestaña Editor
    3. Pegar `[click](javascript:alert(1))` en original, traducir — preview no ejecuta script
    4. Verificar paneles Original|Traducido renderizados tras traducir
    5. Expandir panel Validación — checks visibles con estados
    6. Confirmar botón Descargar activo aunque haya validation error (simular rompiendo fence en mock si necesario)
    7. Reducir viewport &lt;768px — previews apilados verticalmente
  </how-to-verify>
  <verify>
    <automated>test -f static/js/app.js && grep -q renderPreview static/js/app.js</automated>
  </verify>
  <done>Usuario confirma preview XSS-safe, panel validation y layout responsive</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser→DOM | HTML generado desde Markdown traducido (XSS surface) |
| browser→API | validation JSON en respuesta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | S | renderPreview XSS | mitigate | DOMPurify obligatorio; FORBID script/iframe; test javascript: links |
| T-02-05-02 | S | CDN supply chain | mitigate | Pin versions jsdelivr; guard undefined marked/DOMPurify |
| T-02-05-03 | I | Validation panel | accept | Solo lectura; no altera traducción |
| T-02-05-04 | D | UI strict block | mitigate | D-01: no bloquear download en UI; CLI --strict separado |
</threat_model>

<verification>
- grep/static checks pass
- Checkpoint humano: XSS inert, layout responsive, panel validation
</verification>

<success_criteria>
- PREV-01: Vista previa renderizada original y traducido
- PREV-02: DOMPurify antes de DOM insert
- VAL-02 (UI): Panel validation visible tras traducir editor/file
</success_criteria>

<output>
After completion, create `.planning/phases/02-trust-qa/02-05-SUMMARY.md`
</output>
