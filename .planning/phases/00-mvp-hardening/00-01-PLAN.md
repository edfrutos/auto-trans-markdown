---
phase: 00-mvp-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:

  - src/translator.py
  - src/main.py

autonomous: true
requirements:

  - HARD-01

must_haves:
  truths:

    - "Traducción con segmentos faltantes falla antes de reassemble, nunca devuelve salida parcial silenciosa"
    - "API responde HTTP 502 con mensaje que indica cuántos segmentos faltan"
    - "Validación de conteo aplica en caminos OpenAI y DeepL"
  artifacts:
    - path: src/translator.py
      provides: "IncompleteTranslationError y validación post-lote"
      contains: "IncompleteTranslationError"
    - path: src/main.py
      provides: "Mapeo de IncompleteTranslationError a HTTP 502"
      contains: "IncompleteTranslationError"
  key_links:
    - from: src/translator.py
      to: translate_segments
      via: "_validate_translation_completeness al final del flujo"
      pattern: "_validate_translation_completeness"
    - from: src/main.py
      to: src/translator.py
      via: "except IncompleteTranslationError → HTTPException(502)"
      pattern: "IncompleteTranslationError"

---

<objective>
Implementar rechazo explícito de traducciones incompletas en traductor y API (HARD-01, D-01 a D-03).

Purpose: Evitar salida «casi traducida» cuando el proveedor devuelve menos segmentos de los solicitados.
Output: Excepción `IncompleteTranslationError`, validación en `translate_segments`, respuesta HTTP 502 estructurada.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-mvp-hardening/00-CONTEXT.md
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@src/translator.py
@src/main.py
@src/parser.py

<interfaces>
From src/translator.py (existing):
```python
def translate_segments(
    items: list[tuple[int, str]],
    target_lang: str,
    source_lang: str | None = None,
    *,
    on_progress: Callable[[int, int], None] | None = None,
    client=None,
) -> dict[int, str]: ...

def _parse_openai_response(raw: str, expected_count: int) -> list[str]: ...
def _translate_deepl_batch(...) -> list[str]: ...
```

From src/main.py (existing pipeline):
```python
async def _translate_file_content(content, target_lang, source) -> str:
    segments = segment_markdown(content)
    translatable = collect_translatable(segments)
    translations = await loop.run_in_executor(None, partial(translate_segments, ...))
    return reassemble(segments, translations)  # MUST NOT run if translations incomplete
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Excepción y validación de completitud en traductor</name>
  <files>src/translator.py, tests/test_translator.py</files>
  <read_first>src/translator.py, tests/test_parser.py, .planning/phases/00-mvp-hardening/00-CONTEXT.md</read_first>
  <behavior>

    - Test: translate_segments con mock OpenAI que devuelve N-1 traducciones lanza IncompleteTranslationError
    - Test: translate_segments con mock DeepL que devuelve lista más corta lanza IncompleteTranslationError
    - Test: translate_segments completo devuelve dict con exactamente los índices solicitados
  </behavior>
  <action>
    Per D-01, D-02, D-03:

    1. Añadir excepción `IncompleteTranslationError(ValueError)` con atributos `expected: int`, `received: int`, `missing_indices: list[int]`.

    2. Añadir `_validate_translation_completeness(items: list[tuple[int, str]], result: dict[int, str]) -> None` que compare el conjunto de índices esperados (`{idx for idx, _ in items}`) con `result.keys()`. Si difieren, calcular `missing_indices = sorted(expected - received)` y lanzar `IncompleteTranslationError(expected=len(expected), received=len(result), missing_indices=missing_indices)`.

    3. Tras el bucle de lotes en `translate_segments`, llamar `_validate_translation_completeness(items, result)` antes del `return`.

    4. En `_translate_deepl_batch`, tras `client.translate_text`, validar explícitamente `len(translated_list) == len(texts)`; si no coincide, lanzar `ValueError` con mensaje de conteo (el validador final también cubrirá índices).

    5. Exportar `IncompleteTranslationError` como símbolo público del módulo (importable desde main).

    6. Crear `tests/test_translator.py` con mocks de cliente OpenAI/DeepL (sin API keys). Usar `monkeypatch.setenv("TRANSLATION_PROVIDER", ...)` y pasar `client=` inyectado a `translate_segments`.

    NO modificar `reassemble` para fallar silenciosamente — la validación ocurre antes de reassemble.
  </action>
  <verify>
    <automated>pytest tests/test_translator.py -q</automated>
  </verify>
  <acceptance_criteria>

    - src/translator.py contiene class IncompleteTranslationError
    - src/translator.py contiene _validate_translation_completeness
    - translate_segments llama al validador antes de return
    - tests/test_translator.py existe con tests de incompletitud OpenAI y DeepL
  </acceptance_criteria>
  <done>IncompleteTranslationError se lanza en ambos proveedores cuando faltan segmentos; tests pasan</done>

</task>

<task type="auto">
  <name>Task 2: Mapear incompletitud a HTTP 502 en API</name>
  <files>src/main.py</files>
  <read_first>src/main.py, src/translator.py</read_first>
  <action>
    Per D-01, D-03:

    1. Importar `IncompleteTranslationError` desde `src.translator`.

    2. Añadir helper `_translation_http_exception(exc: IncompleteTranslationError) -> HTTPException` que devuelva status 502 con `detail` dict:
       - `"message"`: texto en español, p.ej. `"Traducción incompleta: faltan {len(missing_indices)} de {expected} segmentos"`
       - `"expected"`: int
       - `"received"`: int
       - `"missing_indices"`: list[int] (per D-03 modo debug)

    3. En `translate_text`, `_translate_file_content` (vía sus callers), capturar `IncompleteTranslationError` **antes** del `except Exception` genérico y re-lanzar como `_translation_http_exception(e)`. Aplicar en los tres handlers: `translate_text`, `translate_file`, `translate_batch`.

    4. Garantizar que `reassemble` nunca se invoca si `translate_segments` lanzó — el flujo actual ya lo cumple si la excepción sube desde executor.

    NO devolver contenido parcial en ningún camino.
  </action>
  <verify>
    <automated>python -c "from src.main import app; from src.translator import IncompleteTranslationError; print('ok')"</automated>
  </verify>
  <acceptance_criteria>

    - src/main.py importa IncompleteTranslationError
    - Los tres endpoints translate_* capturan IncompleteTranslationError y devuelven HTTPException(502, ...)
    - detail incluye claves message, expected, received, missing_indices
  </acceptance_criteria>
  <done>API nunca devuelve Markdown parcial cuando faltan traducciones; error 502 es explícito</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                  | Description                                       |
| ------------------------- | ------------------------------------------------- |
| proveedor MT → traductor  | Respuesta del proveedor puede omitir segmentos    |
| traductor → API → cliente | Cliente no debe recibir salida parcial como éxito |

## STRIDE Threat Register

| Threat ID   | Category   | Component                       | Disposition   | Mitigation Plan                                                                    |
| ----------- | ---------- | ------------------------------- | ------------- | ---------------------------------------------------------------------------------- |
| T-00-01-01  | I          | translate_segments / reassemble | mitigate      | Validar índices completos antes de reassemble; 502 si incompleto (D-01)            |
| T-00-01-02  | I          | HTTP 502 detail                 | accept        | missing_indices expone índices de segmento, no contenido; bajo riesgo en uso local |
</threat_model>

<verification>

- [ ] `pytest tests/test_translator.py -q` pasa
- [ ] Import de app y traductor sin errores
- [ ] Ningún handler devuelve 200 con traducciones parciales

</verification>

<success_criteria>

- HARD-01 cumplido: traducciones incompletas → excepción → HTTP 502 sin salida parcial
- Validación en OpenAI y DeepL
- Tests unitarios del traductor cubren incompletitud

</success_criteria>

<output>
After completion, create `.planning/phases/00-mvp-hardening/00-01-SUMMARY.md`
</output>
