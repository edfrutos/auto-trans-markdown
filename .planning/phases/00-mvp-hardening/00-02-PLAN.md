---
phase: 00-mvp-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "00-01"
files_modified:
  - src/main.py
autonomous: true
requirements:
  - HARD-02
must_haves:
  truths:
    - "Upload de bytes no UTF-8 válidos devuelve HTTP 400 con mensaje claro en español"
    - "No existe fallback latin-1 ni errors=replace en decodificación de archivos"
    - "Ruta de editor (JSON) también rechaza contenido vacío; uploads validados en archivo único y lote"
  artifacts:
    - path: src/main.py
      provides: "_decode_upload UTF-8 estricto"
      contains: "UnicodeDecodeError"
  key_links:
    - from: src/main.py
      to: translate_file / translate_batch
      via: "_decode_upload en lectura de UploadFile"
      pattern: "_decode_upload\\("
    - from: src/main.py
      to: translate_text
      via: "validación de contenido no vacío (ya existente)"
      pattern: "El contenido está vacío"
---

<objective>
Eliminar decodificación permisiva y rechazar uploads no UTF-8 con HTTP 400 (HARD-02, D-04 a D-06).

Purpose: Evitar traducciones silenciosas con caracteres sustitutos () en documentos mal codificados.
Output: `_decode_upload` estricto UTF-8; mensajes de error accionables en las tres rutas de entrada.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-mvp-hardening/00-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/main.py

<interfaces>
Existing _decode_upload (to replace):
```python
def _decode_upload(raw: bytes, filename: str) -> str:
    ...
    try:
        return raw.decode("utf-8")
    except UnicodeDecodeError:
        return raw.decode("latin-1", errors="replace")  # REMOVE per D-05
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UTF-8 estricto en _decode_upload</name>
  <files>src/main.py, tests/test_api.py</files>
  <read_first>src/main.py, .planning/phases/00-mvp-hardening/00-CONTEXT.md</read_first>
  <behavior>
    - Test: bytes UTF-8 válidos decodifican correctamente
    - Test: bytes con secuencia inválida (p.ej. b"\\xff\\xfe") lanzan ValueError con mensaje que menciona UTF-8
    - Test: POST /api/translate/file con bytes inválidos → HTTP 400
    - Test: POST /api/translate/batch con un archivo inválido → HTTP 400
  </behavior>
  <action>
    Per D-04, D-05, D-06:

    1. Modificar `_decode_upload`: mantener checks de vacío y binario (`\\x00`). En `UnicodeDecodeError`, **eliminar** fallback `latin-1`/`replace`. En su lugar:
       ```python
       raise ValueError(
           f"{filename}: el archivo no es UTF-8 válido. "
           "Guarda el archivo con codificación UTF-8 e inténtalo de nuevo."
       ) from e
       ```

    2. Crear esqueleto `tests/test_api.py` con `TestClient(app)` de `fastapi.testclient`. Tests mínimos de UTF-8 para file y batch (mock `translate_segments` o monkeypatch para no llamar proveedor en tests de decode — la decodificación ocurre antes de traducir).

    3. Verificar que `translate_file` y `translate_batch` ya convierten `ValueError` de `_decode_upload` en `HTTPException(400, str(e))` — no cambiar ese patrón.

    NO reintroducir ningún fallback de codificación alternativa.
  </action>
  <verify>
    <automated>pytest tests/test_api.py -k utf -q</automated>
  </verify>
  <acceptance_criteria>
    - src/main.py NO contiene "latin-1" ni 'errors="replace"'
    - _decode_upload lanza ValueError en UnicodeDecodeError
    - tests/test_api.py contiene tests de upload inválido → 400
  </acceptance_criteria>
  <done>Uploads no UTF-8 fallan con 400; latin-1 eliminado</done>
</task>

<task type="auto">
  <name>Task 2: Validación coherente en las tres rutas de entrada</name>
  <files>src/main.py</files>
  <read_first>src/main.py, tests/test_api.py</read_first>
  <action>
    Per D-06:

    1. Confirmar que `translate_text` ya rechaza contenido vacío con 400 — mantener.

    2. Añadir test en `tests/test_api.py`: POST `/api/translate` con content válido UTF-8 pasa validación de vacío (mock translate_segments para respuesta rápida).

    3. Documentar en docstring de `_decode_upload` que editor usa JSON (UTF-8 implícito en Python 3); la validación estricta de bytes aplica a multipart file/batch.

    4. Ejecutar grep de regresión: ningún `decode(` en main.py excepto utf-8 explícito.

    NO añadir validación redundante que rechace caracteres Unicode válidos en JSON.
  </action>
  <verify>
    <automated>pytest tests/test_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_api.py incluye test de translate_text con mock
    - grep confirma ausencia de latin-1 en src/main.py
    - pytest tests/test_api.py pasa
  </acceptance_criteria>
  <done>Las tres rutas (editor, archivo, lote) tienen contrato UTF-8/vacío coherente</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cliente → upload multipart | Bytes arbitrarios del usuario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | T | _decode_upload | mitigate | Rechazar UTF-8 inválido con 400 explícito (D-04) |
| T-00-02-02 | I | latin-1 fallback | mitigate | Eliminar sustitución silenciosa (D-05) |
</threat_model>

<verification>
- [ ] `pytest tests/test_api.py -q` pasa
- [ ] `rg "latin-1|errors=.replace" src/main.py` sin coincidencias
</verification>

<success_criteria>
- HARD-02 cumplido: uploads no UTF-8 → HTTP 400, sin sustitución silenciosa
- Tests API cubren file y batch con bytes inválidos
</success_criteria>

<output>
After completion, create `.planning/phases/00-mvp-hardening/00-02-SUMMARY.md`
</output>
