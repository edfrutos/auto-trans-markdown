---
phase: 00-mvp-hardening
plan: 03
type: execute
wave: 3
depends_on:

  - "00-01"
  - "00-02"

files_modified:

  - src/translator.py
  - src/main.py
  - static/index.html
  - static/js/app.js

autonomous: true
requirements:

  - HARD-03

must_haves:
  truths:

    - "/api/languages devuelve solo idiomas soportados por TRANSLATION_PROVIDER activo"
    - "target_lang y source_lang inválidos devuelven HTTP 400 en /translate, /translate/file y /translate/batch"
    - "UI carga idiomas desde API sin lista hardcodeada desalineada con el backend"
  artifacts:
    - path: src/translator.py
      provides: "get_supported_language_codes, validación source/target por proveedor"
      contains: "get_supported_language_codes"
    - path: src/main.py
      provides: "list_languages filtrado y _validate_languages en rutas"
      contains: "get_supported_language_codes"
    - path: static/js/app.js
      provides: "loadLanguages consume /api/languages"
      contains: "/api/languages"
  key_links:
    - from: src/main.py
      to: src/translator.py
      via: "get_supported_language_codes() en list_languages y validación"
      pattern: "get_supported_language_codes"
    - from: static/js/app.js
      to: "/api/languages"
      via: "fetch en loadLanguages"
      pattern: "fetch\\('/api/languages'\\)"

---

<objective>
Filtrar idiomas por proveedor activo y validar source/target en todas las rutas API; alinear UI (HARD-03, D-07 a D-09).

Purpose: Evitar errores 502/503 en runtime cuando la UI ofrece idiomas que DeepL no soporta (p.ej. catalán).
Output: Helpers de idioma en traductor, validación centralizada en main, UI sin opciones estáticas conflictivas.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-mvp-hardening/00-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/translator.py
@src/main.py
@static/js/app.js
@static/index.html

<interfaces>
From src/translator.py:
```python
LANGUAGE_NAMES: dict[str, str]
DEEPL_TARGET_MAP: dict[str, str]
DEEPL_SOURCE_MAP: dict[str, str]
def get_provider() -> str: ...
```

DeepL supported targets: keys of DEEPL_TARGET_MAP (~28 langs).
OpenAI: full LANGUAGE_NAMES (~30+ langs including ca, gl, eu not in DeepL).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Helpers de idioma por proveedor en traductor</name>
  <files>src/translator.py, tests/test_translator.py</files>
  <read_first>src/translator.py, tests/test_translator.py</read_first>
  <behavior>

    - Test: get_supported_language_codes() con provider openai incluye "ca"
    - Test: get_supported_language_codes() con provider deepl NO incluye "ca" pero sí "es"
    - Test: is_valid_source_lang("auto") True; is_valid_source_lang("xx") False
    - Test: is_valid_target_lang("ca") False cuando provider=deepl
  </behavior>
  <action>
    Per D-07, D-08:

    1. Añadir `get_supported_language_codes(provider: str | None = None) -> frozenset[str]`:
       - `deepl` → `frozenset(DEEPL_TARGET_MAP.keys())`
       - `openai` (default) → `frozenset(LANGUAGE_NAMES.keys())`

    2. Añadir `get_supported_languages(provider: str | None = None) -> dict[str, str]` filtrando LANGUAGE_NAMES por códigos soportados.

    3. Añadir `is_valid_target_lang(code: str, provider: str | None = None) -> bool`.

    4. Añadir `is_valid_source_lang(code: str, provider: str | None = None) -> bool`:
       - `"auto"` → True siempre
       - `deepl` → code in DEEPL_SOURCE_MAP
       - `openai` → code in LANGUAGE_NAMES

    5. Extender tests/test_translator.py con casos openai vs deepl usando monkeypatch de TRANSLATION_PROVIDER.

    NO hardcodear listas duplicadas — derivar de DEEPL_TARGET_MAP / LANGUAGE_NAMES existentes.
  </action>
  <verify>
    <automated>pytest tests/test_translator.py -k language -q</automated>
  </verify>
  <acceptance_criteria>

    - src/translator.py exporta get_supported_language_codes, is_valid_target_lang, is_valid_source_lang
    - Tests confirman ca ∈ openai, ca ∉ deepl
  </acceptance_criteria>
  <done>Helpers de idioma disponibles y testeados por proveedor</done>

</task>

<task type="auto">
  <name>Task 2: Filtrar /api/languages y validar en todas las rutas</name>
  <files>src/main.py, tests/test_api.py</files>
  <read_first>src/main.py, src/translator.py, tests/test_api.py</read_first>
  <action>
    Per D-07, D-08:

    1. Importar `get_supported_languages`, `is_valid_target_lang`, `is_valid_source_lang` desde translator.

    2. Añadir `_validate_languages(target_lang: str, source_lang: str) -> None` que lance `HTTPException(400, ...)` en español si target o source (≠ auto) no son válidos para el proveedor activo.

    3. Refactorizar `list_languages`:
       - Mantener entrada `auto` para source
       - Listar solo `get_supported_languages()` ordenado por nombre
       - NO incluir códigos fuera del proveedor activo

    4. Llamar `_validate_languages` al inicio de `translate_text`, `translate_file`, `translate_batch` (antes de segmentar/traducir).

    5. Reemplazar check actual `body.target_lang not in LANGUAGE_NAMES` por validación unificada.

    6. Tests en test_api.py:
       - GET /api/languages con monkeypatch TRANSLATION_PROVIDER=deepl → respuesta sin "ca"
       - POST /api/translate target_lang=ca con deepl → 400
       - POST /api/translate/file source_lang=xx → 400
       - POST /api/translate/batch target_lang=invalid → 400

    Mock translate_segments en tests de éxito para evitar llamadas reales.
  </action>
  <verify>
    <automated>pytest tests/test_api.py -k lang -q</automated>
  </verify>
  <acceptance_criteria>

    - list_languages usa get_supported_languages
    - translate_text, translate_file, translate_batch llaman _validate_languages
    - tests confirman 400 por idioma inválido en multipart y JSON
  </acceptance_criteria>
  <done>API expone y valida solo idiomas del proveedor activo en todas las rutas</done>

</task>

<task type="auto">
  <name>Task 3: Alinear UI con /api/languages dinámico</name>
  <files>static/index.html, static/js/app.js</files>
  <read_first>static/js/app.js, static/index.html</read_first>
  <action>
    Per D-09:

    1. En `static/index.html`, eliminar opciones hardcodeadas de idioma destino (p.ej. `<option value="es">español</option>`) dejando selects vacíos o solo placeholder mínimo; source puede conservar solo `<option value="auto">`.

    2. En `loadLanguages()` (`static/js/app.js`):
       - Poblar target y source desde respuesta API (ya parcialmente implementado)
       - Si fetch falla, mostrar mensaje de error visible (no depender de HTML estático con lista completa desalineada)
       - Seleccionar primer idioma destino disponible si "es" no está en la lista (p.ej. con DeepL sin ca pero con es)

    3. Verificar que `loadLanguages()` se invoca al init de la app (ya debería).

    NO hardcodear listas de idiomas en JS — solo consumir API.
  </action>
  <verify>
    <automated>rg -n "value=\"ca\"|value=\"gl\"|value=\"eu\"" static/index.html static/js/app.js; test $? -ne 0 || echo "check hardcoded langs"</automated>
  </verify>
  <acceptance_criteria>

    - static/index.html no contiene opciones de idioma destino hardcodeadas excepto auto en source
    - loadLanguages maneja error de fetch sin usar fallback con lista completa incorrecta
    - app.js no define arrays estáticos de LANGUAGE_NAMES
  </acceptance_criteria>
  <done>UI refleja dinámicamente las capacidades del proveedor activo</done>

</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary                | Description                   |
| ----------------------- | ----------------------------- |
| cliente → API Form/JSON | Códigos de idioma arbitrarios |

## STRIDE Threat Register

| Threat ID   | Category   | Component             | Disposition   | Mitigation Plan                                                  |
| ----------- | ---------- | --------------------- | ------------- | ---------------------------------------------------------------- |
| T-00-03-01  | I          | translate_* endpoints | mitigate      | Validar source/target contra proveedor antes de llamar MT (D-08) |
| T-00-03-02  | I          | list_languages        | mitigate      | Filtrar por get_supported_languages (D-07)                       |
</threat_model>

<verification>

- [ ] `pytest tests/test_translator.py tests/test_api.py -q` pasa
- [ ] GET /api/languages coherente con TRANSLATION_PROVIDER
- [ ] UI sin listas hardcodeadas desalineadas

</verification>

<success_criteria>

- HARD-03 cumplido: idiomas filtrados por proveedor en API y UI; validación en las 3 rutas

</success_criteria>

<output>
After completion, create `.planning/phases/00-mvp-hardening/00-03-SUMMARY.md`
</output>
