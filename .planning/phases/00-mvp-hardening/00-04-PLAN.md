---
phase: 00-mvp-hardening
plan: 04
type: execute
wave: 4
depends_on:
  - "00-01"
  - "00-02"
  - "00-03"
files_modified:
  - tests/conftest.py
  - tests/test_translator.py
  - tests/test_api.py
  - tests/test_integration.py
  - pyproject.toml
autonomous: true
requirements:
  - HARD-04
must_haves:
  truths:
    - "Tests de integración cubren translate_segments mockeado, reassemble y endpoints FastAPI sin API keys reales"
    - "Casos obligatorios D-11 pasan: JSON incompleto→502, bytes inválidos→400, idioma inválido→400, round-trip índices"
    - "tests/test_parser.py existente sigue pasando sin regresiones"
  artifacts:
    - path: tests/conftest.py
      provides: "Fixtures TestClient y mocks de proveedor"
    - path: tests/test_translator.py
      provides: "Tests traductor con mocks"
    - path: tests/test_api.py
      provides: "Tests HTTP contratos HARD-01/02/03"
    - path: tests/test_integration.py
      provides: "Round-trip segment→translate→reassemble"
  key_links:
    - from: tests/test_integration.py
      to: src/parser.py
      via: "segment_markdown + reassemble"
      pattern: "reassemble\\(segments"
    - from: tests/test_api.py
      to: src/main.py
      via: "TestClient POST /api/translate"
      pattern: "TestClient\\(app\\)"
---

<objective>
Completar suite de integración con proveedor mockeado cubriendo traductor, API y contrato reassemble (HARD-04, D-10 a D-12).

Purpose: Red de seguridad automatizada antes de ampliar funcionalidad en Phase 1.
Output: conftest compartido, tests consolidados, pytest declarado en pyproject.toml, suite verde.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-mvp-hardening/00-CONTEXT.md
@tests/test_parser.py
@src/parser.py
@src/translator.py
@src/main.py

Prior plans implement HARD-01/02/03 — this plan consolidates and extends test coverage per D-11 mandatory cases.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fixtures compartidas y dependencia pytest</name>
  <files>tests/conftest.py, pyproject.toml</files>
  <read_first>tests/test_parser.py, pyproject.toml, src/main.py</read_first>
  <action>
    Per D-10, D-12:

    1. Añadir `pytest>=8.0` a `[project.optional-dependencies] test = ["pytest>=8.0"]` en pyproject.toml (o a dependencies si el proyecto prefiere instalación directa — usar optional-dependencies test).

    2. Crear `tests/conftest.py` con:
       - `client` fixture: `TestClient(app)` importando `from src.main import app`
       - `mock_translate_success` fixture: monkeypatch `src.main.translate_segments` para devolver traducciones 1:1 por índice
       - `mock_translate_incomplete` fixture: devuelve dict con un índice faltante → debe provocar 502 vía IncompleteTranslationError del traductor real OR mock que lanza IncompleteTranslationError
       - Helper `make_md_bytes(text: str) -> bytes` y `invalid_utf8_bytes() -> bytes`

    3. Documentar en docstring de conftest que tests NO requieren OPENAI_API_KEY ni DEEPL_API_KEY.

    Preferir monkeypatch de `translate_segments` en capa main para tests API; tests de traductor mockean clientes OpenAI/DeepL directamente.
  </action>
  <verify>
    <automated>pytest tests/conftest.py --collect-only 2>/dev/null; pytest --collect-only tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py existe con fixture client
    - pyproject.toml declara pytest en optional-dependencies test
    - pytest --collect-only tests/ encuentra tests
  </acceptance_criteria>
  <done>Infraestructura de tests lista para integración sin API keys</done>
</task>

<task type="auto">
  <name>Task 2: Consolidar tests traductor y API (casos D-11)</name>
  <files>tests/test_translator.py, tests/test_api.py</files>
  <read_first>tests/test_translator.py, tests/test_api.py, .planning/phases/00-mvp-hardening/00-CONTEXT.md</read_first>
  <action>
    Per D-11:

    Asegurar cobertura explícita (añadir tests faltantes si plans 01-03 dejaron huecos):

    **test_translator.py:**
    - Respuesta JSON incompleta OpenAI → IncompleteTranslationError o ValueError propagado
    - DeepL batch corto → IncompleteTranslationError
    - Round-trip: segment_markdown → translate_segments (mock) → reassemble preserva fences y traduce solo segmentos traducibles

    **test_api.py:**
    - POST /api/translate con traducción incompleta → status 502, detail con expected/received
    - POST /api/translate/file upload UTF-8 inválido → 400
    - POST /api/translate con target_lang inválido → 400
    - POST /api/translate/batch con source_lang inválido → 400
    - POST /api/translate éxito con mock → 200 y content traducido

    Usar fixtures de conftest. Mensajes de error en español donde aplique (assert substring "UTF-8" o "no soportado").

    NO llamar APIs externas reales.
  </action>
  <verify>
    <automated>pytest tests/test_translator.py tests/test_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - test_api.py contiene test de 502 por traducción incompleta
    - test_api.py contiene tests 400 UTF-8 e idioma inválido
    - test_translator.py contiene test round-trip con reassemble
  </acceptance_criteria>
  <done>Todos los casos obligatorios D-11 cubiertos en tests automatizados</done>
</task>

<task type="auto">
  <name>Task 3: Test de integración end-to-end del pipeline</name>
  <files>tests/test_integration.py</files>
  <read_first>src/parser.py, src/translator.py, tests/conftest.py</read_first>
  <action>
    Per D-10, D-11 (round-trip índices):

    1. Crear `tests/test_integration.py` con test `test_pipeline_segment_translate_reassemble`:
       - Input MD con fence + texto traducible (reutilizar patrón de test_parser.py)
       - Mock translate_segments que mapea cada índice a prefijo "TR:"
       - Verificar output contiene fence intacto y textos traducidos
       - Verificar len(collect_translatable) == len(translations mock devuelve)
       - Verificar índices únicos post-reassemble (no duplicar líneas)

    2. Test `test_api_translate_text_integration`: TestClient + mock translate_segments, verificar segments_total y segments_translated en JSON.

    3. Ejecutar suite completa incluyendo parser existente.

    Mantener tests/test_parser.py sin modificaciones salvo que falle por cambio incompatible (no debería).
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_integration.py existe con pipeline round-trip
    - pytest tests/ -q pasa (parser + translator + api + integration)
    - Ningún test requiere variables OPENAI_API_KEY o DEEPL_API_KEY
  </acceptance_criteria>
  <done>HARD-04 cumplido: suite de integración verde sin API keys reales</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → tests | Tests no deben filtrar secretos ni llamar MT real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04-01 | I | test suite | mitigate | Mocks obligatorios; sin network a OpenAI/DeepL (D-10) |
| T-00-04-02 | D | CI pytest | accept | Tests locales; no DoS — scope unitario |
</threat_model>

<verification>
- [ ] `pytest tests/ -q` — todos pasan
- [ ] Casos D-11 presentes y verdes
- [ ] tests/test_parser.py sin regresiones
</verification>

<success_criteria>
- HARD-04 cumplido: integración traductor + API + reassemble testeada con mocks
- Suite ejecutable con `pytest tests/ -q` sin credenciales
</success_criteria>

<output>
After completion, create `.planning/phases/00-mvp-hardening/00-04-SUMMARY.md`
</output>
